너는 오늘의수학 저장소의 워크트리에서 일한다.
작업 디렉터리: C:\Users\user\orca\workspaces\testautocreator\그림벡터검수

# 네 일 — 그림을 **보고** 판정하는 것 하나뿐이다

`scripts/qa/reports/svg-compare/` 에 비교 시트 PNG 가 약 **1,576장** 있다.
각 장은 **왼쪽이 래스터(정본), 오른쪽이 SVG** 이고, 둘 다 실제 인쇄 폭(mm)으로
브라우저가 그린 것이다. 목록은 같은 폴더의 `index.json` 에 있다.

한 장씩 **열어 보고**, 오른쪽 SVG 가 왼쪽 래스터를 충실히 담고 있는지 판정해라.

## 찾을 것 (수치 검사가 구조적으로 못 보는 것들이다)

- 획이 **빠졌다** — 선분, 화살표 머리, 눈금, 해칭, 점
- 글자가 **빠졌거나 깨졌다** — 꼭짓점 이름(A·B·C), 치수(`16 cm`), 축 이름, 각도, 단위
- 선 굵기가 **부풀었다/사라졌다**
- **잘렸다** — 테두리 밖으로 나간 부분
- 채움이 **까맣게 뭉갰다**
- **전혀 다른 그림**이다

⚠️ 비율(가로세로)은 **이미 수치로 걸렀다.** 「비율이 맞으니 괜찮다」는 답이 아니다.
⚠️ SVG 가 래스터보다 **더 또렷한 것은 정상이다**(벡터니까). 결함이 아니다.
⚠️ 글꼴 모양이 조금 다른 것도 정상이다. **내용이 빠졌는지**를 봐라.

## 방법

1. 파일 이름 번호순(0001 부터)으로 **차례대로** 봐라. 시트는 이미 무작위로
   섞여 있으니 순서대로 보면 그게 곧 무작위 표본이다.
2. 다 못 보면 **몇 번까지 봤는지 반드시 적어라.** 「N장 확인」은 N 이 전부인지
   말해 주지 않는다. 중간에 그만두더라도 **본 데까지의 결함률**을 적어라.
3. 결함을 찾으면 그 시트의 `index.json` 항목에서 `svg` 경로를 찾아 적어라.

## 산출물 — 둘 다 만들어라

### ① `scripts/qa/reports/svg-blocklist.txt`
결함으로 판정한 **SVG 경로만** 한 줄에 하나. 주석은 `#` 로. 예:
```
# 0042: 삼각형 꼭짓점 이름 A·B·C 가 전부 빠졌다
public/figures-svg/3512/q07.svg
```
이 파일이 그대로 차단 목록으로 쓰인다. **경로를 정확히 적어라.**

### ② `docs/planning/tracks/report-figure-svg-verify.md`
- **본 장수 / 전체 1,576** (분모를 먼저)
- 결함 건수·비율, 부류별 집계
- **기출 vs RPM** 갈라서 (index.json 의 `kind`)
- 🔴 한 줄 결론: 전환해도 되나 / 안 되나 / 조건부인가

## 지킬 것

- **아무것도 바꾸지 마라** — DB, `public/` 의 파일. 판정만 한다.
- 비교 시트 PNG 는 **커밋하지 마라**(용량). 위 산출물 둘만 커밋한다.
- `git add -A` 금지. 경로로만 스테이징.
- 다 못 봐도 좋다. **본 만큼 정확히 보고하는 것**이 전부 봤다고 말하는 것보다 낫다.
