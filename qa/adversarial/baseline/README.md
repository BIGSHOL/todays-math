# 적대적 리뷰용 **기준선 사본** — 손대지 말 것

`git show 5c0b1400:src/...` 를 그대로 떠 놓은 것이다(임포트 경로만 상대 경로로 고쳤다).
`5c0b1400` 은 본문 조판 수리(`424fa6dd`~`f5c81303`)가 시작되기 **직전**의 main —
`docs/planning/tracks/reports/body-typeset.md` 가 «수리 전» 이라고 부르는 그 커밋이다.

왜 필요한가: 보고서의 「상자·하위문항·등식 판정에 안 걸리는 44,000건은 한 글자도
안 바뀐다」를 **전수로** 확인하려면 옛 출력이 있어야 한다. 옛 출력을 지금 코드로
재구성하면(플래그를 끄는 식으로) 그건 동어반복이다 — 옛 코드 자체가 있어야 한다.

재현: `npx tsx qa/adversarial/scripts/scan-render-drift.ts`
