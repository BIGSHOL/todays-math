# dependencies
node_modules/

# next.js
.next/
out/
build/

# env — 비밀정보 커밋 금지 (07-coding-convention §5)
.env
.env.local
.env*.local
.env.test

# testing
coverage/
playwright-report/
test-results/

# prisma
prisma/*.db

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.pnp
.pnp.*
.vercel
*.tsbuildinfo
next-env.d.ts

# editor
.idea/
.vscode/

# worktree (프로젝트 내부 생성 방지용 — 실제 worktree는 상위 폴더에 생성)
worktree/

# T3.0 이관 덤프 — 집계 마크다운만 커밋
scripts/import/reports/*.json
.tmp-orca/
.qa-artifacts/

# QA 감사 상세 리포트(대용량, 재생성 가능) — 통과 원장(ocr-audit-clean.txt)만 커밋한다
# 재생성 가능한 산출물 — 하위 디렉터리(index-batch/, final-batch/)까지 전부.
# `*.json` 한 겹만 무시하면 배치 산출물 수백 개가 추적되지 않은 채 남아 리베이스를 막는다.
scripts/qa/reports/*
# ⚠️ 디렉터리 자체(`reports/`)를 무시하면 **아래 `!` 예외가 먹지 않는다** — git 은 제외된
# 디렉터리 안의 파일을 되살리지 못한다. 그래서 `/*` 로 내용물만 무시한다.
# (2026-08-18 적대적 리뷰: 잠금 원장이 커밋되지 않아 다른 컴퓨터에서 되돌릴 수 없었다.)
# 조사 산출물은 예외 — 다시 뽑으려면 수 분이 걸리고 다른 컴퓨터가 바로 읽어야 한다.
!scripts/qa/reports/topic-coverage.json
# 그림 유실 잠금 원장 — **공유 DB 를 되돌리는 유일한 근거다.**
# 여기 없으면 다른 컴퓨터에서 790건을 되돌릴 방법이 없다(적대적 리뷰 지적).
!scripts/qa/reports/missing-figure-lock.json
# 중복 문항 삭제 원장 — **지운 행의 모든 컬럼**이 여기에만 있다.
# 이게 없으면 `--revert` 가 아무것도 못 되살린다(영구 삭제가 된다).
!scripts/qa/reports/duplicate-delete-ledger.json
# AI 생성 문항 보기 표기 정규화 — 되돌리기 원장(공유 DB 를 바꾼다)
!scripts/qa/reports/view-normalize-ledger.json
# 정답 정정 목록 — **이 목록 자체가 되돌리기 근거다**(`before`→`after`).
# `apply-answer-corrections.ts` 가 이걸 읽어 공유 DB 를 바꾸는데, 커밋이 안 되고
# 있었다. 다른 컴퓨터에서는 「무엇을 왜 바꿨나」를 알 길이 없다(2026-08-19 발견,
# 그때 이미 3행이 적용돼 있었다).
!scripts/qa/reports/answer-corrections.json
# RPM 출처 스냅샷 — **sumaek 을 대신하는 정본이다.** 이게 없으면 다른 컴퓨터에서
# RPM 의 책·쪽·문항번호를 알 길이 없다(원본 접속은 저장소 밖 두 파일에 의존한다).
!scripts/qa/reports/rpm-origin.json
# 그 적재의 되돌리기 근거.
!scripts/qa/reports/rpm-origin-load.json
# 해설 감사 결과 — 「원본에도 없다 201건」의 근거. 다시 뽑으려면 정답책 6권이 필요하다.
!scripts/qa/reports/rpm-solution-audit.json
# 정답책 글꼴 조사 — 변환기 매핑의 근거. 다시 뽑으려면 정답책 6권이 필요하다.
!scripts/qa/reports/rpm-glyph-census.json
# □ 로 무너진 517행의 대조표 — 원장님 판단 근거. 다시 뽑으려면 정답책 6권이 필요하다.
!scripts/qa/reports/rpm-square-compare.json
# □ 되살리기 — **되돌리기 원장**. 공유 DB 를 바꾸므로 반드시 커밋된다.
!scripts/qa/reports/rpm-square-repair-ledger.json
!scripts/qa/reports/rpm-square-repair-ledger2.json
!scripts/qa/reports/rpm-square-verified.json
!scripts/qa/reports/rpm-book-fill-ledger.json
!scripts/qa/reports/rpm-content-repair-ledger.json
!scripts/qa/reports/rpm-content-repair2-ledger.json
!scripts/qa/reports/rpm-square-repair3-ledger.json
!scripts/qa/reports/rpm-content-verified.json
!scripts/qa/reports/rpm-damage-census.json
!scripts/qa/reports/rpm-book-fill-verified.json
!scripts/qa/reports/unusable-discard-lock.json
!scripts/qa/reports/rpm-number-check.json
# 문항 코드 원장(D-53) — 컬럼을 드롭했다 다시 만들 때 **같은 코드**를 되살리는 근거다.
# 코드는 원장님이 종이·대화에 옮겨 적는 값이라 새로 뽑으면 다른 문항을 가리키게 된다.
!scripts/qa/reports/problem-code-ledger.json.gz

# 본문 교체 백업 — **공유 DB 본문 34행을 되돌리는 유일한 근거다.**
# 그림 URL 과 달리 본문은 다시 만들어 낼 수 없다(HWP 추출본이 있어야 재생성된다).
# 25KB 라 커밋에 부담이 없다.
!scripts/qa/reports/hwp-replace-backup.json
# 회수 그림 되붙이기 원장 — **공유 DB 를 되돌리는 유일한 근거다** (행마다 `before`).
# 이게 없으면 다른 컴퓨터에서 되돌릴 방법이 없다.
!scripts/qa/reports/figure-attach-ledger.json
# 보기 그림 짝 적재 원장 — **공유 DB 를 되돌리는 유일한 근거다** (행마다 `before`).
# 여기 없으면 다른 컴퓨터에서 97건을 되돌릴 방법이 없다.
!scripts/qa/reports/choice-figure-index-apply.json
# 그림 물리 폭(mm) 적재 원장 — **공유 DB 를 되돌리는 유일한 근거다** (행마다 `before`).
!scripts/qa/reports/figure-source-mm-apply.json
# 그림 적용 트랙 단계 0 측정 — 보고서가 인용하는 분모·출제 영향.
!scripts/qa/reports/figure-apply-stage0.json
# 300dpi 바꿔치기 계획과 **되돌리기 원장** — 원장에 문항별 figure_dims 의
# 적용 전/후가 있다. 이게 없으면 치수를 되돌릴 방법이 없다.
!scripts/qa/reports/figure-swap-plan.json
!scripts/qa/reports/figure-swap-ledger.json
# SVG 채택 되돌리기 원장 — figureUrls 의 **옛 경로**가 여기에만 있다.
!scripts/qa/reports/figure-svg-adopt.json
# 단원 오배정 되돌리기 원장.
!scripts/qa/reports/basic-figure-unit-fix.json
# 날 LaTeX 문항 제외 되돌리기 원장 — 고친 뒤 되돌릴 근거가 여기에만 있다.
!scripts/qa/reports/latex-leak-reject.json
# 글자 오독 되돌리기 원장 — 본문을 바꾼 유일한 근거다.
!scripts/qa/reports/glyph-misdecode.json
# 단계 3 허용 목록 — 「눈으로 본 시트」가 여기 적혀 있다. 이게 없으면 무엇을
# 근거로 채택했는지 다음 사람이 못 센다.
!scripts/qa/reports/svg-whitelist.txt
# 2차 합본 허용 목록 — 「무엇을 근거로 채택했나」가 여기에만 있다.
!scripts/qa/reports/svg-whitelist-2.txt
# 뭉친 문항 되돌리기 원장 — 「무엇을 왜 뺐나」가 여기에만 있다.
!scripts/qa/reports/merged-problem-reject.json
# 보기 그림 짝을 못 찾아 **출제에서 뺀** 문항 원장 — 행마다 이전 상태와 사유.
# 영구 삭제가 아니라 되살릴 수 있어야 하고, 그 근거가 이 파일뿐이다.
!scripts/qa/reports/choice-figure-discard-lock.json

# RPM 그림 되붙이기 **되돌리기 원장** — 공유 DB 를 바꾸는 스크립트의 undo 다.
# 이 컴퓨터에만 남으면 되돌릴 길이 없다(2026-08-18 실측).
!scripts/qa/reports/rpm-figure-attach-ledger.json
# 정답과 보기가 안 맞는 문항 전량 목록 — 원장님이 «빼자»를 정하는 근거다.
# DB 없는 컴퓨터에서도 바로 읽혀야 한다 (162KB).
!scripts/qa/reports/unusable-problems.json
# HWP 원본 회수의 행 단위 판정 — 291건 전량의 «회복/부분/못살림»과 그 근거.
# 보고서의 모든 숫자가 이 파일에서 나오고, 원장님이 «복구냐 AI 냐»를 정하는 근거다.
# DB 도 N드라이브도 없는 컴퓨터에서 바로 읽혀야 한다 (164KB).
!scripts/qa/reports/hwp-rescue-ledger.json
# 정답 표기가 갈리던 5건을 HWP 미주로 확정한 기록 — **되돌릴 근거가 이 파일뿐이다.**
# 행마다 옛 정답·미주 원문·왜 그 읽기인지를 담는다(일률 규칙이 아니라 목록이다).
!scripts/qa/reports/ambiguous-answer-ledger.json
# HWP 재추출본으로 본문을 교체한 기록 — **행마다 바꾸기 전 본문**이 들어 있다.
# 되돌릴 근거가 이 파일뿐이다(영구 삭제가 아니라 교체다).
!scripts/qa/reports/hwp-rescue-apply-ledger.json
__pycache__/

# 본문 백업(대용량, 되돌리기용)
scripts/qa/reports/transformed-content-backup.json

# 에이전트 임시 스크립트 (_ 로 시작)
scripts/qa/_*

# verify-convert-rpm --fault 가 만드는 변환기 임시 사본.
# 정상 종료 시 스스로 지우지만, 중간에 죽으면 남는다. 절대 커밋되면 안 된다
# — 일부러 망가뜨린 변환기다.
src/lib/import/_convertRpm.fault.*.ts

# 폐기 후보 기계용 목록(문서는 커밋)
scripts/qa/reports/discard-candidates.json

# 트랙 G(소단원 분류) 중간 산출물 — 재생성 가능. 판정 결과와 실측치만 남긴다.
scripts/classify/reports/labeled.jsonl
scripts/classify/reports/target.jsonl
scripts/classify/reports/units-db.json
scripts/classify/reports/db-labels.jsonl
scripts/classify/reports/loaded-external-ids.json

# 지면 실측 캐시 (scripts/qa/measure-*.tsx --json) — 재생성 가능, 수 MB
.measure/

# 300dpi 재크롭 산출 — 대용량. 견준 뒤에 바꿔치기한다. 커밋하지 마라.
# (다중 세션이 git add -A 로 쓸어 담지 못하게 막는다.)
public/figures-300/
public/figures-300-enhanced/
# 바꿔치기(단계 2)로 밀려난 **옛 파일**의 보관소. git 이 public/figures 를 추적하므로
# 되돌리기의 진짜 근거는 이력이다 — 이건 곁다리 안전망이라 커밋하지 않는다
# (커밋하면 같은 그림이 저장소에 두 벌 쌓인다).
public/figures-backup-300swap/

# RPM 원본 교재 PDF (내려받은 것 — 저장소에 넣지 않는다)
.rpm-src/

# HWP → PDF 변환 산출물 (재생성 가능, 저장소에 넣지 않는다)
.hwp-pdf/

# 임시 조사용 (커밋하지 않는다)
.probe/
!scripts/qa/reports/question-type-unify-ledger.json

# 그림 칸 원장(㉮) — 「이 그림이 원본 지면에서 몇 mm 였나」의 유일한 자리.
# 300dpi 재크롭·인쇄 물리 크기·벡터 SVG 목표 크기가 전부 이 값을 쓴다.
# 다시 만들려면 원본 PDF 전량을 다시 매핑해야 해서 20분 넘게 걸린다.
!scripts/qa/reports/figure-rect-ledger.json
# RPM 오려내기 계획 3개 — 원장의 「자리 못 구함 600」을 메우는 근거.
# 앞 세션이 「이 컴퓨터에 없다」고 적었던 바로 그 파일이다.
!scripts/qa/reports/rpm-crop-plan-gated.json
!scripts/qa/reports/rpm-crop-plan-main667.json
!scripts/qa/reports/rpm-crop-plan-recrop1646.json
# 그 600장을 계획+디스크 PNG 로 되찾은 원장.
!scripts/qa/reports/rpm-rect-fill.json
# 벡터 SVG 전량 산출 결과 — 다시 뽑으려면 Chromium 전량이 필요하다.
!scripts/qa/reports/figure-vector-svg-result.json

# 이 트랙의 임시 산출물 (커밋하지 않는다)
.work/
# 그림 인쇄 크기 트랙 2차 — 15mm 미만 53장을 원본 지면에서 조사한 결과.
# 보고서가 이 숫자를 인용하므로 **원장 없이도 검산되게** 커밋한다
# (원장 figure-rect-ledger.json 도 위에서 커밋한다 — 다시 만들려면 원본 PDF
#  전량을 20분 넘게 다시 매핑해야 한다).
!scripts/qa/reports/figure-tiny-inspect.json

# 「본문방향 검수 시트」 — **사람이 보고 적은 판정**과 그 근거 좌표는 커밋한다.
# 이게 없으면 「누가 무엇을 보고 왜 그렇게 정했나」가 사라지고, 다시 만들 수도 없다.
# (칸 그림 `rpm-stem-sheet/` 9MB 는 재생성 가능하므로 안 넣는다.)
!scripts/qa/reports/rpm-stem-sheet-decision.json
!scripts/qa/reports/rpm-stem-sheet.json

# 「무리 짝짓기 검수 시트」 — 같은 이유로 커밋한다. 판정이 «덩어리 3» 처럼 **번호로**
# 적혀 있어서, 그 번호가 무엇을 가리키는지는 시트에만 있다 — 시트가 없으면 판정이
# 뜻을 잃는다. (띠 그림 `rpm-pair-sheet/` 는 재생성 가능하므로 안 넣는다.)
!scripts/qa/reports/rpm-pair-decision.json
!scripts/qa/reports/rpm-pair-sheet.json

# 「쪽 장식 제외」로 새로 오린 18건의 판정 — **사람이 무엇을 보고 「쓴다」고 했는지**가
# 여기에만 있다. 붙이는 쪽(`apply-rpm-furniture.py`)이 이 파일을 읽어 대상을 좁히므로,
# 없으면 다른 컴퓨터에서 그 실행이 **멈춘다**(조용히 전량을 붙이지 않게 일부러 그렇게 했다).
!scripts/qa/reports/rpm-furniture-decision.json
scripts/qa/reports/svg-compare/
scripts/qa/reports/svg-compare-blocked/
# SVG 채택 차단 목록 — 적용 스크립트가 이걸 읽는다. PNG 시트는 커밋하지 않는다.
!scripts/qa/reports/svg-blocklist.txt

# 「안티그래비티가 건 결함 9건을 확대해서 대조한 판정」 — **사람이 무엇을 보고
# 「거짓」이라 했는지**가 여기에만 있다. 이게 없으면 다음 사람이 같은 9건을
# 다시 확대해서 다시 본다(그림 파일은 재생성되므로 판정만 남기면 된다).
!scripts/qa/reports/svg-agy-flags-verdict.txt

# 빈 분수 되살리기·제외 원장 — **되돌리기가 이 파일 하나를 읽는다.** 커밋 안 되면
# 이 컴퓨터에서만 되돌릴 수 있다(2026-08-18 에 실제로 그랬다).
!scripts/qa/reports/empty-frac-repair.json
!scripts/qa/reports/empty-frac-reject.json
!scripts/qa/reports/lim-arrow-repair.json
!scripts/qa/reports/solution-hwp-repair.json
