/**
 * **사람이 눈으로 보고 판정한 `미분류` 행.** id → 무엇을 보고 무엇이라 정했나.
 *
 * `report-missing-figures.ts` 는 `미분류` 가 0이 아니면 「목록이 아직 눈멀었다」고 경고한다.
 * 그 경고가 쓸모 있으려면 **이미 사람이 본 것**과 **아직 아무도 안 본 것**이 갈려야 한다 —
 * 안 그러면 같은 5건 때문에 경고가 늘 켜져 있고, 그러면 아무도 안 본다.
 *
 * ⚠️ **여기 적는 것은 잠금·해제에 쓰이지 않는다.** 판정은 `missingFigureRule.ts` 하나뿐이고,
 *    이 목록은 「누가 언제 보고 무엇이라 했나」를 남기는 자리다. 낱말로 못 가르는 부류
 *    (가로등 그림자 — 그림이 붙어 있는 것이 15건이다)를 규칙으로 «불필요» 라고 단정하면
 *    멀쩡한 문항이 조용히 빠진다(2026-08-18 적대적 리뷰). 그래서 규칙은 `미분류` 로 두고
 *    사람의 판정만 여기 적는다.
 */
export const REVIEWED_UNCLASSIFIED: Record<string, string> = {
  // 2026-08-19 · 트랙 기출원본회수 · 미분류 20건 전량 육안 검토
  "5262-1":
    "그림 불필요 — 용어 빈칸 채우기. 「좌표평면 위에 점·직선·곡선 등으로 나타낸 그림」은 «그래프»의 뜻풀이다",
  "4590-16":
    "그림 불필요 — 가로등 그림자. 175cm·3.5m·1m/s 수치만으로 풀린다. 낱말로는 못 가르는 부류라 규칙은 미분류로 둔다",
  "2973-5":
    "그림 불필요 — 가로등 그림자. 1.6m·8m·3m/s 수치만으로 풀린다. 위와 같은 부류",
  "4105-4":
    "그림 불필요 — 「산점도에서 점들이 …」는 선택지 안의 개념 서술이다. 지면에 산점도가 없어야 정상",
  "3517-3": "그림 불필요 — 좌표·그래프 용어를 묻는 개념 문항",
};
