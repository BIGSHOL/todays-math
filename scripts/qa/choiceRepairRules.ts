/**
 * R2 규칙의 **QA 쪽 창구**. 구현은 제품에 있다 — 여기서 다시 쓰지 않는다.
 *
 * 원장님 확정(2026-08-19)으로 R2 가 제품 파서에 들어갔다. 규칙이 두 벌이 되면
 * 「32건」이 두 트랙에서 갈라져도 아무도 모른다. 그래서 한 벌만 두고 여기서는
 * 이름만 이어 준다.
 */
export { splitInlineChoiceMarkers } from "../../src/lib/problem/choiceRepairRules";
