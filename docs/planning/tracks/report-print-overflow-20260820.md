# 인쇄 넘침 실측 — 2026-08-20 (SVG 채택 뒤)

> 잰 것: `npx tsx scripts/qa/measure-print-overflow.tsx --json .measure/cont-20260820b.json`
> 캐시·지문: `.measure/cont-20260820b.json` · `.measure/cont-20260820b.manifest.json`
> 인수인계 브리프 §3.ⓕ 가 「SVG 세션이 끝난 뒤 다시 재라」고 미뤄 둔 것이다.

## 0. 한 줄

**경고 재현율 98.7% · 놓친 것 20건(0.04%).** 그리고 **SVG 채택으로 새로 넘치게
된 문항은 0건**이다.

---

## 1. 전수 실측 (이어지는 장 · 문항 칸 484px)

| | 값 |
|---|---:|
| 잰 문항 | 47,014 |
| 실측 넘침 | **1,528 (3.25%)** |
| ★ 넘치는데 경고 없음 | **20 (0.04%)** |
| 경고인데 안 넘침 | 455 (0.97%) |
| 경고 재현율 | **98.7%** |

★ 이 학생에게 그대로 인쇄돼 나가는 부류다. 가장 크게 넘친 것이 66px 이고
전부 「추정 19~23줄」이라 **자의 줄 수 추정이 경계에서 한두 줄 모자란** 것이다.

> ⚠️ **이 값을 「판정의 성적」으로 물려받지 말 것.** 이건 **오늘 DB 의** 성적이다.
> 그림 치수가 안 채워진 문항이 들어오면 달라진다(CLAUDE.md 2026-08-18).

## 2. SVG 채택이 넘침을 악화시켰나 — **아니다**

되돌리기 원장(`figure-svg-adopt.json`)의 1,399문항을 **채택 전 캐시**
(`.measure/cont.json`, 2026-08-20 15:00)와 **채택 뒤 캐시**로 맞대어 잰 값이다.

| | 건수 |
|---|---:|
| 두 측정에 다 있는 것 | 1,399 |
| 높이가 **그대로** | 1,379 |
| 늘어남 | 16 |
| 줄어듦 | 4 |
| **🔴 새로 넘치게 된 것** | **0** |
| 넘침이 해소된 것 | 0 |

평균 변화 **+0.01px** · 최대 +40.7px(한 문항). 지금 넘치는 15건은 **채택 전에도
넘치고 있었다.**

인쇄 폭은 mm 에서 오므로 가로는 한 픽셀도 안 변하고, 세로만 viewBox 비율을
따라간다(가드 2% 안). 실측이 그 보장과 맞았다.

## 3. 캐시 지문이 제 일을 했다

재려고 `--verify` 를 먼저 돌렸더니 이렇게 멈췄다:

```
Error: 캐시 47049건 vs DB 47014건 — 표본으로 되살릴 수 없다. 전수로 다시 재라.
```

DB 가 그 사이 35행 줄었다(중복 삭제·뭉친 문항 제외). 지문이 없었으면 **낡은
캐시로 «96%» 를 다시 찍었을 것**이다 — 적대적 리뷰 ④ F 가 잡은 바로 그 자리다.

## 4. 다음

- 놓친 20건은 **자의 줄 수 추정**이 경계에서 모자란 부류다. 더 내리려면
  선정 정책이 아니라 `estimateProblemPx` 를 손봐야 한다(2026-08-18 §「도달할 수
  없는 목표」와 같은 자리 — 목표는 0 이 아니라 **자의 놓침률**이다).
- **첫 장**(`--first-page`)은 아직 이 회차로 안 쟀다. 이어지는 장과 칸 높이가
  달라 따로 재야 한다.
