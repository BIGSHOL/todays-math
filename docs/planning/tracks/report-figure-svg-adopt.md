# 단계 3 — 벡터 SVG 채택 (부분 적용)

> 워크트리 `그림적용` · 브랜치 `feat/figure-apply` · 2026-08-20
> 검수 근거: `scripts/qa/reports/svg-whitelist.txt` · 되돌리기: `scripts/qa/reports/figure-svg-adopt.json`

## 0. 한 줄

**눈으로 본 810자리 중 결함 7을 빼고 716문항 / 727자리를 SVG 로 바꿨다.**
나머지 766자리는 **결함이 아니라 아직 안 본 것**이라 래스터로 남는다.

---

## 1. 왜 「차단」이 아니라 「허용」인가

원래 설계는 「검수가 결함으로 찍은 것만 빼고 나머지 다 채택」이었다.
그런데 **전량 검수가 중간에 끊겼다** — 병렬 세션 넷 중 둘이 계정 세션 한도에
걸려 죽었다. 그 상태로 차단 목록을 쓰면 **안 본 자리가 조용히 들어온다.**

안 본 자리에 무엇이 있는지는 정의상 모른다. 그리고 본 구간에서 실제로
**「전혀 다른 그림」**(`1410`)이 나왔다 — 비율·잉크량 같은 수치 가드를
**전부 통과한 채로**. 그러니 못 본 것은 결함이 아니라 **아직 근거가 없는
것**이고, 근거 없이 지면에 내보내지 않는다.

`decideAdoption` 에 `whitelist` 를 넣어 뒤집었다(`--whitelist=<파일>`).
빈 목록은 「목록 없음」이 아니라 **「아무것도 안 본다」**로 읽어 전량을 막는다.

---

## 2. 분모를 먼저

| | 수 |
| --- | ---: |
| 채택 후보 그림 자리 | 1,576 |
| **눈으로 본 자리** | **810** (52.7%) — 시트 135 / 263 |
| 그중 확정 결함 | 7 |
| 허용 목록 | 803 |
| **실제로 바뀐 것** | **716문항 · 727자리** |
| 반올림 뒤 비율이 어긋나 막힌 자리 | 3 |
| 아직 안 본 자리 (래스터로 남음) | 766 |

본 구간과 **누가 봤는지**는 허용 목록 머리에 적혀 있다 — 시트 1~66·133~198은
병렬 세션, 67·114·199는 직접.

---

## 3. 확정 결함 7 — 두 부류로 갈린다

**내용이 틀린 것 (반드시 막아야 한다)**

| | 무엇이 |
| --- | --- |
| `1410` | **전혀 다른 그림** — 정본은 원 O·36°·14cm, SVG 는 내접사각형·80°·70° |
| `1524` | 원뿔 세 토막 중 C 의 칠이 빠졌다 |
| `0485` | 좌표축 이름 `x`·`y` 와 화살촉이 잘렸다 |
| `0263` | 상자그림 A반 왼쪽 수염의 **끝 표시**가 빠졌다 (최솟값을 읽는 표시다) |

**보기만 나빠진 것 (문제 푸는 데는 지장 없다)**

| | 무엇이 |
| --- | --- |
| `0510` · `1283` | 사람·양 삽화가 검은 덩어리로 뭉갰다 |
| `0480` | 피라미드 벽돌 무늬가 사라졌다 (형태·치수·라벨·점선은 온전) |

셋 다 **사진 같은 삽화**다. 개별 사고가 아니라 벡터화가 색 층을 뭉개는
**한 가지 성질**로 보인다 — 남은 766자리에서 더 나올 것이다.
지금은 셋 다 뺐다. 래스터 쪽이 더 나으니 바꿀 이유가 없다.

---

## 4. 🔴 이 트랙에서 잡은 결함 둘 — 둘 다 「조용히」 틀렸다

### ⑴ 목록 파서가 줄 끝 주석을 안 뗐다

허용 목록에 `경로  # 1390` 처럼 번호를 달았는데, 파서가 그 줄을 통째로
경로로 읽었다. 어떤 경로와도 안 맞아 **「바꿀 것 0」** 이 나왔다.

**차단 목록이었으면 에러 없이 결함 SVG 가 그대로 지면에 나갔다** — 숫자만
조용히 달라진다. 허용 목록이라 0이 되어 드러났다(뒤집은 덕을 여기서 봤다).
차단·허용이 **같은 함수**(`readPathList`)를 쓰게 하고 테스트로 잠갔다.

### ⑵ 가드는 반올림 **전**을 재고, 지면은 반올림 **후**를 쓴다

`figure_dims` 는 `Int[]` 다. viewBox 가 74×29 처럼 작으면 **반올림만으로
비율이 3.07% 흔들린다** — 가드는 2%다. 즉 「2% 안」이라는 보장이 지면에서는
거짓이었다. **세는 쪽과 쓰는 쪽이 다른 값을 보는** 그 자리다.

가드가 **저장할 값**을 재게 고쳤다. 자리 3개가 새로 막혔고, 인쇄 높이
최대 변화가 −2.98% → **−1.85%** 로 들어왔다.

이걸 찾은 것은 `scripts/qa/measure-svg-height-delta.mjs` 하나다 —
「가드가 보장한다는 것을 지면 값으로 다시 재 보는」 자다. 그 자가 없었으면
숫자가 맞아 보였다.

---

## 5. 지면이 얼마나 움직였나

인쇄 **폭**은 `figure_source_mm` 에서 오므로 **한 픽셀도 안 변한다.**
높이만 비율을 따라간다.

| | |
| --- | ---: |
| 높이 변화 중앙 | 0.414% |
| 95분위 | 1.139% |
| 최대 | **−1.85%** (가드 상한 2% 안) |
| 1% 넘게 변한 자리 | 68 / 727 (9.4%) |

---

## 6. 못 한 것 · 다음

- **766자리(47.3%)를 아직 안 봤다.** 계정 세션 한도가 18:20 에 풀린다.
  이어서 시트 68~113 · 115~132 · 200~263 을 훑고 **허용 목록에 더하기만**
  하면 된다 — 되돌릴 일이 없는 구조다.
- `1152` 는 확대해서 깨끗한 것을 확인했지만 **그 시트가 아직 안 훑였다.**
  규칙을 하나로 두려고 목록에 안 넣었다. 다음 회차에 들어온다.
- **종이에서 선이 연한지 모른다.** `/dev/print-check` `figure-svg-adopt` 가
  `"대기"` 다 — 원장님 실물 검수 항목이다.
- 비율로 막힌 171자리는 그대로 막혀 있다. 그중 일부는 **래스터가 나쁜 크롭**
  이라 그림 회수 쪽 일감이다.

---

## 7. 되돌리기

```
ALLOW_SHARED_IMPORT=1 npx tsx scripts/qa/adopt-figure-svg.ts --revert --apply
```

원장(`figure-svg-adopt.json`)은 **DB 보다 먼저** 쓰고 커밋된다
(`git check-ignore` 로 확인했다). 되돌리기는 지금 값이 자기가 쓴 값일 때만 되돌린다.
