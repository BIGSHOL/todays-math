# 그림 벡터 트랙 — 보고서

> 워크트리 `그림벡터` · 브랜치 `feat/figure-vector` · 지시서
> `docs/planning/tracks/brief-figure-vector.md`
> **작업 도중에도 계속 갱신한다.** 마지막에 몰아 쓰지 않는다.

---

## 0. 진행 상황

| 항목 | 상태 |
| --- | --- |
| §1 비용 ① 분모 | ✅ 측정 완료 (아래 §1) |
| §1 비용 ② 시간 | ✅ 10편 실측 → 외삽 → **전량 실측으로 검산**까지 (아래 §2) |
| §1 비용 ③ 네이티브/폴백 | ✅ 표본이 아니라 **전량** (아래 §3) |
| §1 비용 ④ 못 재는 것 | ✅ 아래 §5 |
| ㉮ 그림 칸 원장 | ✅ 16,119장 · rect 14,329 (88.9%) (아래 §4 · §11) |
| ㉯ 벡터 → SVG | ✅ 2,505장 전량 · 통과 2,467 · 버림 38 (아래 §12) |
| ㉰ 눈 대조 | ✅ 통과「문제 없음」·버림·RPM/기출 고루 · `<img>` 80/80 (아래 §12) |

### 0.1 이어받기 (2026-08-19, 앞 세션 한도 사망)

`git log --oneline main..HEAD` 10개. 원장·SVG 파이프라인·획손실 자·`<use>` 굵기·기출 벡터=표 정정까지는 커밋돼 있다.

**죽은 지점의 미커밋 수정 — 이어서 끝낸다** (되돌리지 않음):

| 파일 | 판단 |
| --- | --- |
| `scripts/figure/build-rect-ledger.py` | `hwppdf-q` 출처를 `figure-attach-ledger.json` 으로 짚은 주석. 사실이라 남긴다. `NO_RECT_KINDS` 바늘이 옛 문구(`hwppdf 계열`)를 보고 있어 새 주석과 안 맞으므로 **같이 고친다.** |
| `scripts/figure/check-svg-as-img.mjs` | `<img src>` 로 띄우는 검사. 지면이 쓰는 방식이라 ㉰에 쓴다. |
| `docs/planning/tracks/brief-figure-vector.md` · `figure-quality-brief.md` | 지시서. 추적 안 된 채 워크트리에만 있었다. 커밋한다. |

`public/figures-svg` 는 이 세션에서 2,467장을 다시 뽑았다(아래 §12).

### 0.2 계획 파일 3개 — 「rect 가 그림 칸」이 **아니다** (실측)

`scripts/qa/reports/rpm-crop-plan-{gated,main667,recrop1646}.json` 은 이 워크트리에 있다. 앞 세션 보고서 §6.1 4번(「이 컴퓨터에 그때 계획이 없다」)은 **틀렸다.**

| | gated | main667 | recrop1646 |
| --- | ---: | ---: | ---: |
| 문항 | 403 | 667 | 1,646 |
| 합집합 | | | **1,854** |
| 자리 못 구한 600 과 겹침 | 30 | 141 | **600 / 600** |
| 겹치는 문항의 rect 불일치 | | | **0 / 545** |

페이지 불일치 49건은 전부 3-2 의 쪽 오프셋 표기 차이: gated 는 `page = origin+3` (pageOff=3), recrop·main 은 origin 쪽 그대로(pageOff=0). **rect 값은 세 파일 모두 `rpm-origin.json` 의 `source_coords`(문항 상자)와 같다.** 그림 칸이 아니다 — 디스크 PNG 치수(`rect × dpi`)와 0건이 맞는다(이미 자리를 구한 1,010장도 0건).

그래서 계획의 rect 를 원장에 그대로 옮겨 적으면 **발문 상자**가 그림 칸이 된다. 계획이 주는 것은 (1) 3-2 쪽 오프셋 +3 (gated) (2) 600장 전부의 원본 PDF·쪽·상자. 그림 칸은 디스크 PNG 를 그 쪽 위에 겹쳐 찾아 증명한다.

---

## 1. 비용 ① — 분모 (실측 2026-08-19)

### 1.1 그림 파일은 16,119장, **네 갈래**다

`find public/figures -type f | wc -l` = **16,119**
(§1 브리프의 16,122 는 `measure-figure-resolution.py` 가 잰 시점 값. 3장 차이는
그 뒤 다른 트랙이 되돌린 분이다 — 이 트랙은 **지금 디스크에 있는 16,119** 를 분모로 쓴다.)

| 갈래 | 장수 | 만든 것 | 원본 지면 rect 를 구할 수 있나 |
| --- | ---: | --- | --- |
| `<examId>/qNN[_i].ext` | 12,925 | `extract-all-figures.py` (PDF 오려내기) | **가능** — `map_exam()` 재실행 |
| `<examId>/hwp-qNN[_i].png` | 1,444 | `recover-hwp-figures.py` (HWP BinData) | **PDF 로는 불가** — 아래 1.3 |
| `<examId>/hwppdf-qNN.png` | 15 | (전용 경로) | 조사 필요 |
| `<examId>/pdf-qNN.png` | 14 | `crop-pdf-by-stem.py` | 가능(별도 경로) |
| `<examId>/tbl-qNN.png` | 3 | `crop-table-by-stem.py` | 가능(별도 경로) |
| `rpm/<externalId>/N.png` | 1,718 | `crop-rpm-from-pdf.py` | **가능** — `figure_rect()` 재실행 |

기출 소계 14,401 (편 2,208) · RPM 소계 1,718 (문항 1,661).

### 1.2 원본 PDF 는 **몇 편이 디스크에 실제로 있나**

기출 그림 2,208편의 원본을 `exam_index.db exams.src_path` 로 물어
**경로마다 `Path.exists()` 를 직접 쳤다**(인벤토리 같은 파생물을 안 봤다 — 2026-08-18
「없다」 셋이 다 있었던 사고 때문).

| 원본 상태 | 편 | 그림 |
| --- | ---: | ---: |
| **PDF 원본이 N드라이브에 있음** | 1,902 | 13,415 |
| HWP 원본만 있음 (`.hwp` 305 · `.hwpx` 1) — **변환 PDF 캐시가 없다** | 306 | 986 |
| PDF 원본이 없음 | **0** | 0 |

→ **PDF 원본이 없어서 못 하는 편은 0편이다.** 막는 것은 HWP 쪽뿐이다.

RPM 교재 6권도 전량 N드라이브에 있다
(`N:\개인\강아\교재자료\RPM\22\RPM 중학 N-N 학생용.pdf`).
쪽수를 대장(`rpm-origin.json`)과 대조했다:

| 책 | 실제 쪽 | 대장 쪽 | 지면 |
| --- | ---: | ---: | --- |
| 1-1 · 1-2 · 2-1 · 2-2 · 3-1 | 184·200·192·192·200 | 같음 | 623.6×841.9 |
| **3-2** | **139** | **136** | 623.6×841.9 |

⚠️ **3-2 만 3쪽 많다.** 지면 크기는 여섯 권 모두 623.6×841.9 로 같아
「여백을 잘라 낸 재저장본」(2026-08-18) 은 아니지만, **쪽 오프셋이 어긋날 수 있다.**
3-2 (그림 대상 문항 505 중 일부) 는 본문 대조로 오프셋을 확인한 뒤에만 쓴다.

### 1.3 못 하는 것 — HWP 계열 그림 1,444장

`hwp-qNN` 은 HWP **BinData 스트림에서 통째로 꺼낸 이미지**다. PDF 좌표가 애초에
없다. 원본 지면 크기를 구하려면 둘 중 하나가 필요하다:

- HWP→PDF 재변환 → **금지**(절대 규칙 9, 원장님 화면 포커스). 게다가 `db/pages` 의
  변환 캐시가 이 컴퓨터에 **없다**(`F:\시험지변환기\db\pages` 디렉터리 자체가 없음).
- HWPX `<hp:pic>` 의 크기 속성(HWPUNIT) — **아직 안 재 봤다.** 조사 예정.

그중 464장은 원본이 `.pdf` 인 편에 섞여 있고, 980장은 `.hwp` 원본 편에 있다.

### 1.4 그래서 원장이 rect 를 채울 수 있는 상한

| 갈래 | 장수 | 비고 |
| --- | ---: | ---: |
| 기출 `qNN` (PDF 원본 있음) | 12,925 | `map_exam()` 재실행 |
| RPM | 1,718 | `figure_rect()` 재실행 |
| **소계 (구할 수 있다고 보는 것)** | **14,643 (90.8%)** | |
| HWP BinData | 1,444 | PDF 경로로는 불가 |
| 기타(`pdf-`/`hwppdf-`/`tbl-`) | 32 | 별도 경로 |

---

## 2. 비용 ② — 시간 (10편 실측 × 외삽)

`map_exam()` 을 **PDF 원본이 있는 편에서 무작위 10편**(seed 11) 뽑아 실제로 돌렸다.

```
10편 5.2s · 편당 평균 0.52s (0.3 ~ 1.0s)
```

**외삽 — 1,902편 × 0.52s/편 = 989s ≈ 16.5분** (근거: 위 10편 실측 × 1,902/10).
⚠️ 이것은 **rect 를 구하는 시간**이지, 다시 오려내 저장하는 시간이 아니다.
300dpi 재크롭 비용은 별도로 재야 한다(아직 안 쟀다).

### 덤 — 매핑이 옛 파일과 같은지 **바이트로 확인했다**

「지금 `map_exam()` 이 주는 rect 가 그때 그 파일의 rect 인가」는 가정이면 안 된다.
네이티브 추출분은 `doc.extract_image(xref)` 의 바이트와 디스크 파일의 md5 를 견줄 수
있다(2026-08-18 「순서는 가정이 아니라 문항마다 확인되는 사실」).

10편 표본: 네이티브 66장 **66장 전부 md5 일치 (100%)** · 불일치 0.
→ 이 갈래는 원장의 `rect` 가 그 파일의 rect 임이 **파일마다 증명된다.**
클립 렌더분 12장은 바이트 대조가 안 된다(렌더는 재현이 아니다) — 픽셀 치수로 검산한다.

---

## 3. 비용 ③ — 네이티브 추출 vs 200dpi 클립 렌더 **(전량 실측)**

§15 가 「`extract-all-figures.py` 가 세기만 하고 안 찍는다」고 한 값이다.
표본이 아니라 **전 편(1,896편)을 다시 매핑해서** 셌다.

| | 장수 | 기출 `qNN` 12,925 중 |
| --- | ---: | ---: |
| **네이티브** (`xref` 로 원본 이미지를 그대로 꺼냄) | **10,779** | 83.4% |
| **클립 렌더 폴백** (200dpi 로 그 영역을 그림) | **2,059** | 15.9% |
| 대응 없음(아래 §5) | 87 | 0.7% |

⚠️ **네이티브 10,779장은 다시 잘라도 화질이 안 는다** — 이미 원본 비트맵 그대로다.
300dpi 재크롭으로 실제 이득이 나는 것은 **클립 렌더 2,059장 + RPM 1,718장**이다.
(브리프 지시대로 확장자로 나누지 않았다. `extract-all-figures.py` 의 `xref` 유무로
갈랐다 — 그게 두 갈래를 만드는 바로 그 분기다.)

### 그리고 **PNG 안에 렌더 해상도가 적혀 있었다**

PyMuPDF 는 `get_pixmap(dpi=D)` 로 그린 PNG 의 `pHYs` 에 D 를 적는다. 되읽으면
`199.9996`·`299.9994` 처럼 딱 떨어지지 않는 값이 나와서, **그 자체가 「우리가 클립
렌더한 것」이라는 지문**이다(원본 이미지를 그대로 꺼낸 PNG 는 `96.012` 처럼 그
이미지 자신의 값이고, JPEG 는 값이 아예 없다).

- 기출 클립 렌더 2,017장: `map_exam` 이 준 rect ↔ 파일에 적힌 dpi 가 **서로 검산**된다.
- **RPM 은 200dpi 만이 아니었다** — 1,239장이 **300dpi**, 479장이 200dpi 다.
  `DEFAULT_DPI = 200` 만 믿고 쟀으면 1,239장의 크기를 전부 1.5배로 잘못 적었을 것이다.
  (처음에 실제로 그렇게 재서 「27%만 맞는다」는 결과가 나왔고, 안 맞는 것들의
  가로세로비가 **정확히 1.5배**여서 드러났다.)

---

## 4. ㉮ 그림 칸 원장 — 결과

산출: `scripts/qa/reports/figure-rect-ledger.json`
(`git check-ignore -q` **exit 1** 확인 — 커밋된다)

### 4.0 전량 합계 (16,119장 = 디스크 파일 전량, **원장에 안 담긴 파일 0**)

| | 장수 | 비율 |
| --- | ---: | ---: |
| **rect(자리+크기) 구함** | **13,698** | **85.0%** |
| **크기(mm)만이라도 구함** | **14,391** | **89.3%** |
| 벡터로 판정 | 1,921 | (rect 구한 것 중 14.0%) |

증명 경로별: 바이트 md5 **10,656** · 픽셀 치수 **2,017** · 원본 지면 겹쳐 대조 **1,025**.

못 구한 2,421장의 **사유별 집계**:

| 사유 | 장수 |
| --- | ---: |
| HWP BinData 계열 — PDF 좌표가 애초에 없다 | 1,444 |
| RPM `figure_rect` 재현 실패 — 그때 쓴 계획이 이 컴퓨터에 없다 | 600 |
| 같은 자리인데 바이트가 다르다 | 123 |
| RPM 무리 그림 — 어느 장이 어느 칸인지 못 가른다 | 93 |
| 지금 `map_exam` 결과에 대응이 없다 | 87 |
| 렌더 치수가 안 맞는다 | 42 |
| `hwppdf-`/`pdf-`/`tbl-` 계열 | 32 |

그리고 **치수 교차검산이 어긋난 34건**도 `note` 에 적어 뒀다(자리는 겹쳐 대조로
확인됐는데 `rect × 렌더dpi` 가 파일 픽셀과 다른 행 — 대개 오려낼 때
`avoid` 로 칸을 깎았기 때문이다. 그런 행은 **크기는 PNG 쪽이 맞다**).

### 4.1 기출 (14,401장 · 실측)

| 판정 | 장수 | 비율 |
| --- | ---: | ---: |
| **rect 구함 — 바이트로 증명** (`extract_image` md5 == 파일 md5) | 10,656 | 74.0% |
| **rect 구함 — 픽셀 치수로 증명** (rect × dpi == 파일 픽셀) | 2,017 | 14.0% |
| 못 구함: HWP BinData 계열(`hwp-qNN`) | 1,444 | 10.0% |
| 못 구함: 바이트 불일치 | 123 | 0.9% |
| 못 구함: 지금 `map_exam` 결과에 대응이 없다 | 87 | 0.6% |
| 못 구함: 렌더 치수 불일치 | 42 | 0.3% |
| 못 구함: `pdf-`/`hwppdf-`/`tbl-` 계열 | 32 | 0.2% |
| **소계 구함** | **12,673** | **88.0%** |

`kind` 는 두 번 돌려도 같았다(raster 11,756 · vector 917) — 배경 이미지 보정은
RPM 에만 영향이 있었다. 클립 렌더 2,017장은 **`map_exam` 의 rect ↔ PNG 에 적힌 렌더
dpi 가 전량(2,017/2,017) 서로 검산**됐다.

### 4.2 벡터/래스터 (§12 의 갈래)

기출에서 rect 를 구한 12,673장 중 **벡터 917장(7.2%)** · 래스터 11,756장.
§12 가 표본(PDF 20편·덩어리 188개)으로 낸 **8.5%** 와 가깝다 — 다른 방법으로 잰
두 값이 맞은 것이라 서로 근거가 된다.

⚠️ **`kind` 를 처음엔 거꾸로 셌다.** 쪽을 덮는 배경 이미지를 그림으로 세서 RPM 이
**전량 raster** 로 나왔다 — §12 실측(RPM 100% 벡터)과 정반대다. 배경을 빼니 표본
13/13 이 벡터가 됐다. 「손상(여기서는 배경)이 정상으로 읽히는 가드」와 같은 자리다.


### 4.3 RPM (1,718장 · 실측)

| 판정 | 장수 | 비율 |
| --- | ---: | ---: |
| **크기(mm) 구함** — PNG 에 적힌 렌더 dpi 로 환산 | **1,718** | **100%** |
| 자리(rect) 구함 — `figure_rect()` 재실행 + 원본 지면과 겹쳐 대조 통과 | 1,025 | 59.7% |
| 자리 못 구함 — 지금 `figure_rect` 가 내는 칸이 디스크 파일과 다르다 | 600 | 34.9% |
| 자리 못 구함 — 무리 그림(한 문항에 여러 장, 어느 장이 어느 칸인지 못 가른다) | 93 | 5.4% |

자리를 구한 1,025장의 `kind` 는 **벡터 1,004(98.0%)** · raster 1 · 판정불가 20.
§12 가 낸 「RPM 표본 100.0% 벡터」와 맞는다.

**자리를 못 구한 600장의 사유**(조사해서 적는다, 추측이 아니다):
그때 오려낸 계획은 `gate-rpm-crop.py` 가 **책별 (dx,dy) 를 맞춰** 낸
`rpm-crop-plan-gated.json` 과 `plan-rpm-group-figures.py` 의 무리 그림 계획인데,
**둘 다 이 컴퓨터에 없다**(`scripts/qa/reports/` 가 gitignore 라 안 따라온다).
표본을 눈으로 보니 어떤 행은 `source_coords` 가 가리키는 자리에 **아예 다른 것**이
있었다(예: 디스크 그림은 삼각형인데 그 좌표에는 「유형 13」 표가 있다) — 몇 pt 어긋난
것이 아니라 **다른 문항의 자리**다. 그래서 (dx,dy) 보정으로는 안 되고, 계획을
다시 만들어야 한다.

---

## 5. ㉯ 벡터 → SVG

### 5.1 거꾸로 간 것 셋 — 전부 실측으로 잡았다

**⑴ 글자를 다시 조판하면 안 된다.** 처음엔 `get_drawings()` 로 획을 옮기고 글자는
`<text>` 로 다시 썼다. 도형은 잘 나왔는데 **글자가 통째로 틀렸다**:

| 원본 | 다시 조판한 결과 |
| --- | --- |
| `℃` | `¾` |
| `1/2` · `-1/3` (분수) | 엉뚱한 낱글자 |
| `3²` | `3U` |

RPM 은 `EHsang`·`EHboNA` 같은 **자체 인코딩 수식 글꼴**이라 텍스트 레이어의
코드포인트가 뜻과 다르다. 기출은 `HyhwpEQ` 가 PUA(``)를 쓴다 — **표본 60건 중
58건**이 그렇다.

🔴 **그리고 이 오류는 겹쳐 대조에 안 걸린다.** `℃` 와 `¾` 는 픽셀 차이가 작아서
평균 절대차 **0.027 로 통과**했다. 「틀린 숫자는 아무 말도 안 한다」(CLAUDE.md
2026-08-19)와 같은 자리다. 그래서 글리프는 `text_as_path=True` 로 **윤곽선 그대로**
옮긴다.

**⑵ MuPDF 는 자기가 낸 SVG 를 자기가 못 읽는다.** `get_svg_image()` 산출물을 다시
열어 그리면 **온통 검다**(픽셀 극값 (0,0)). 그걸로 대조하면 전량이 「어긋났다」가
되고, 반대로 문턱을 낮추면 **아무것도 안 걸린다** — 판정자가 눈이 먼 채로 초록을
찍는 자리다. 그래서 **지면과 같은 렌더러(헤드리스 Chromium)** 로 그려 대조한다.
헤드리스라 원장님 화면 포커스를 안 뺏는다(절대 규칙 9).

**⑶ 온통 검던 진짜 원인은 쪽 배경 PNG 였다.** RPM 은 쪽마다 704×1123 배경 이미지를
까는데 그게 **거의 전부 0(검정)** 이다. 칸을 덮는 이미지는 ㉮ 원장의 `kind` 판정이
이미 「배경」으로 가르고 있었으므로 **같은 기준**으로 여기서도 뺀다.
덤으로 원본 크롭에 있던 **대각선 워터마크가 사라진다**.

### 5.2 쪽 SVG 를 솎는다

브리프 §2 가 경고한 대로 `get_svg_image()` 는 칸으로 잘라도 **쪽 전체**를 적고
`clipPath` 로 가릴 뿐이다(실측 443KB · path 414 · use 954).
`scripts/figure/svg_prune.py` 가 요소마다 bbox 를 계산해 칸 밖을 쳐낸다 —
`d` 는 명령을 제대로 훑는다(숫자만 짝지어 읽으면 `H`/`V` 에서 x·y 가 뒤바뀐다).
**애매하면 남긴다**(bbox 를 넉넉히 잡는다) — 잘못 지운 획은 지면에서 티가 안 난다.

실측: 443KB → **32KB** (92% 감소).

### 5.3 크기와 선 굵기 (§9)

- 인쇄 폭 = `min(70mm, rect 폭(pt)/72*25.4)` — ㉮ 원장이 준다.
  SVG 파일이 `width="…mm"` 로 **스스로 물리 크기를 들고 다닌다**(§14).
- 선 굵기는 **엔진 정본에서 읽어 온다**(`vendor/figure-engine/core/figure_svg.py`
  의 `stroke-width` 리터럴 — 못 읽으면 멈춘다). 실선 1.4 / 보조선 1.0 을
  **mm 로 환산**해 넣어 그림마다 물리 굵기가 같아지게 한다.
- ⚠️ **흰 선은 안 건드린다.** 배경 위에 칸을 나누려고 덧그린 «지우개»라 굵히면
  옆 칸 내용을 지운다(표 그림의 칸 구분선이 전부 흰 선이다).

🔴 **못 한 것 — 글자 크기는 `type_scale` 로 통일하지 못한다**(화질 브리프 §11 ⑥).
글리프가 윤곽선이라 크기를 바꾸려면 다시 조판해야 하고, 다시 조판하면 §5.1 ⑴ 의
틀린 글자가 돌아온다. **내용의 정확성을 택했다.** 선 굵기는 정규화한다 —
그건 내용을 안 바꾼다.

### 5.4 가드를 망가뜨려 봤다

| 변이 | 결과 |
| --- | --- |
| 쪽 배경 이미지를 빼지 않는다 | 표본 6건 **전부 버림** · 겹쳐 대조 0.94 (정상은 0.012) |
| 엔진에서 선 굵기를 못 읽으면 | `SystemExit` 로 **멈춘다**(손으로 적은 값으로 조용히 안 넘어간다) |
| `extract-all-figures.py` 의 `CLIP_DPI` 를 못 찾으면 | `SystemExit` 로 멈춘다 |

---

## 6. 비용 ④ — **못 잰 것 · 못 한 것** (가장 중요한 항목)

### 6.1 못 잰 것

1. **300dpi 재크롭의 시간과 디스크 용량.** 이 트랙이 잰 20~28분은 **rect 를 구하는**
   시간이지 다시 잘라 저장하는 시간이 아니다. 화질 브리프 §6 이 요구한
   「600dpi 전량 재크롭 용량」도 **안 쟀다.**
2. **HWP 계열 1,444장의 원본 지면 크기.** HWPX `<hp:pic>` 의 HWPUNIT 크기 속성으로
   구할 수 있을지 **조사만 적어 두고 재지 못했다.**
3. **바이트 불일치 123건이 「다른 그림」인지 「같은 그림의 다른 인코딩」인지** —
   표본 12건만 열어 봤다(아래 6.3). 전량은 안 봤다.
4. **RPM 자리 못 찾은 600건의 진짜 자리.** ~~이 컴퓨터에 그때 계획이 없어 못 구했다.~~
   **정정 (이어받은 세션):** 계획 3개가 메인 워크트리에 있었고 여기 있다.
   다만 그 `rect` 는 그림 칸이 아니라 `source_coords` 다(§0.2). 그림 칸은
   디스크 PNG 를 원본 쪽 전체에 겹쳐 찾고 있다(아래 §11).
5. **인쇄 지면에서 실제로 어떻게 보이는지.** 이 트랙은 `printOverflow.ts` 를 안
   건드렸다(다른 세션 작업 중). 그래서 **실물 검수는 못 했다** — 절대 규칙 6 대상이다.

### 6.2 못 한 것

1. **글자 크기 통일**(`type_scale`) — §5.3 참조. 윤곽선이라 못 한다. 하려면 다시
   조판해야 하고, 다시 조판하면 틀린 글자가 돌아온다.
2. **`hwppdf-qNN` 15장의 출처** — 어느 스크립트가 만들었는지 못 짚었다.
3. **SVG 를 지면에 물리는 일** — 새 디렉터리(`public/figures-svg/`)에 만들어 두기만
   했다. 바꿔치기는 §14(인쇄가 픽셀이 아니라 mm 를 보게 하는 일)와 함께 가야 한다.

### 6.3 「바이트가 다르다 123건」을 표본 12건 열어 봤다

`extract_image` 로 다시 꺼낸 바이트와 디스크 파일을 **픽셀로** 견줬다:

| | 건수 | 뜻 |
| --- | ---: | --- |
| 크기부터 다르다 | 5 | 그 자리에 **다른 그림**이 있다 |
| 픽셀 차이 0.2~0.85% | 7 | 같은 그림인데 **JPEG 재인코딩**된 것으로 보인다 |

그리고 별도로, 불일치·대응없음 252건에서 40건을 뽑아 **그 PDF 전체 이미지의 md5** 를
훑었더니 **38건은 원본 어디에도 없었다.** 즉 지금 N드라이브에 있는 PDF 는 그때 오려낼
때 쓴 `db/pages/<eid>/src.pdf` **캐시와 다른 파일**이다(그 캐시는 이 컴퓨터에 없다).
바이트가 맞는 10,656건은 두 파일이 그 이미지에 대해서는 같다는 뜻이다.

⚠️ 「픽셀이 거의 같으니 rect 를 인정하자」는 **안 했다.** 0.85% 차이가 재인코딩인지
다른 그림인지 12건으로는 못 가르고, 이 저장소의 규칙대로 **판정은 버리는 쪽**으로
둔다. 회수 상한은 123건 중 약 70건(0.5%)이다.

---

## 7. §14 — 인쇄 쪽에 무엇을 넘기나 (이 트랙은 **정하기만** 한다)

인쇄 코드(`src/lib/printOverflow.ts`)는 **다른 세션이 고치는 중**이라 안 건드렸다.
넘길 것만 정해 둔다.

- **어디에 두나 → 별도 대장.** `scripts/qa/reports/figure-rect-ledger.json` 의
  `figure` 키가 `public/figures` 기준 상대 경로이고, DB `figure_urls` 는
  `/figures/...` 를 담는다. 앞에 `/figures/` 만 붙이면 **바로 조회된다.**
  파일 이름에 mm 를 박는 방법은 경로가 바뀌어 DB 갱신이 따라와야 하므로 안 쓴다.
- **무엇을 넘기나** — `width_mm` · `height_mm`. 인쇄 폭은
  `min(70, width_mm)` 이다(§9).
- **SVG 는 스스로 들고 다닌다** — `width="…mm" height="…mm"` 로 나간다.
- **덮이는 범위** — 14,391 / 16,119 (89.3%). 나머지 1,728장은 mm 를 모르므로
  지금 규칙(픽셀 → 70mm 상한)을 그대로 둘 수밖에 없다. 그 사실을 **세어서** 넘긴다.

---

## 8. 곁가지 — 브리프가 말한 것과 다른 것

- 브리프 §3.6 이 「`fs.rmSync` 금지 — `noRmSync.test.ts` 가 막는다」고 했는데
  **이 워크트리에 그 파일이 없다**(`find . -name "*rmSync*"` 0건). 다른 가지에만
  있는 듯하다. 이 트랙은 `fs.rmSync` 를 안 썼고(파이썬은 파일 단위 `unlink`),
  그 사실을 **테스트가 아니라 내가 확인**했다는 뜻이므로 적어 둔다.
- 커밋 전에 `npm run type-check`(통과) 와 `npm run lint`(**에러 0** · 기존 경고 7,
  전부 이 트랙이 안 건드린 파일)를 직접 돌렸다.
- 그림 파일 수가 브리프의 16,122 가 아니라 **16,119** 다. 이 트랙은 지금 디스크에
  있는 수를 분모로 썼다.

---

## 9. 🔴 브리프의 전제 하나를 정정한다 — **기출의 「벡터 917장」은 도형이 아니다**

브리프 ㉯ 와 화질 브리프 §12 는 「기출의 8.5%(≈1,200장)가 벡터라 SVG 로 뽑으면
그 무리에서 «선이 끊긴다·굵기가 다르다»가 구조적으로 사라진다」고 봤다.
**벡터인 것은 맞는데, 그 벡터가 도형이 아니다.**

### 재 보고 알았다 — 그리고 처음 쓴 자는 틀렸다

처음엔 「글자 면적 비율」로 갈랐다(20% 이상이면 상자). 표본을 **눈으로 보니**
문턱 아래(=도형이라던 쪽) 10건이 **전부 확률분포표·조건 상자·보기 상자**였다.
문턱이 아니라 **축이 틀렸다** — 표도 상자도 글자가 있고 도형에도 라벨이 있다.

열쇠를 바꿨다: **표·상자는 가로·세로 직선뿐이고 도형에는 굽거나 비스듬한 획이 있다.**

| | 굽거나 비스듬한 획이 5% 이상 | 거의 전부 가로·세로 직선 | 그 비율 중앙값 |
| --- | ---: | ---: | ---: |
| 기출 벡터 (표본 160) | **3 (1.9%)** | 157 (98.1%) | **0.000** |
| RPM 벡터 (표본 160) | **155 (96.9%)** | 5 (3.1%) | **0.783** |

그리고 「굽은 획이 있다」로 걸린 기출 8건도 눈으로 보니 **화살표 흐름도·나무그림·
대각선 머리칸 표·둥근 모서리 조건 상자**였고, 진짜 도형은 **1건**(집 모양 A/B/C/D)뿐이었다.

### 그래서 무엇이 달라지나

- **㉯ 의 도형 수확은 사실상 RPM 쪽이다.**
- 기출 917장의 SVG 화도 **값이 없지는 않다** — 지금 그것들은 **글자를 200dpi 로
  래스터화한 표·상자**라 지면에서 흐리다. SVG 가 되면 글자가 또렷해진다.
  다만 「도형의 선이 끊긴다」를 고치는 것은 아니다.
- 기출의 **진짜 도형은 여전히 래스터**(§12 의 91.5%)이고, 그쪽은 300dpi 재크롭
  (화질 트랙 ㉢)이 맡아야 한다.

---

## 10. 가드 둘이 **거꾸로 걸려 있었다** — 둘 다 눈으로 찾았다

### 10.1 「획이 사라졌나」자가 **번짐을 재고 있었다**

전량 실행에서 262건을 버렸는데, 표본을 눈으로 보니 **242건이 원본과 똑같았다.**
「획 손실 54%」로 버려진 그림이 화면에서 구별이 안 됐다.

원인: 픽셀 대 픽셀로 셌다. 원본은 200dpi 래스터라 가는 선이 번져 있고 SVG 는
또렷해서, **같은 선이 반 픽셀만 어긋나도 «사라졌다»** 가 된다.

고침: 한 픽셀 부풀린 뒤 견준다. 그리고 문턱을 **대조군으로** 정했다 —
낸 SVG 의 그릴 요소를 절반 지운 변이본 24장을 만들어 견줬다
(`scripts/figure/make-svg-loss-control.py`):

| | 획손실 최소 | 중앙 | 최대 | 겹쳐대조 최소 | 중앙 | 최대 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 정상본 24장 | 0.000 | 0.005 | 0.061 | 0.007 | 0.018 | 0.033 |
| **절반 지운 24장** | **0.250** | 0.383 | 0.862 | **0.020** | 0.036 | 0.131 |

⚠️ **겹쳐 대조만으로는 안 갈린다** — 망가뜨린 것의 최소(0.020)가 멀쩡한 것의
최대(0.033)보다 **작다.** 획 손실 자가 따로 있어야 하는 이유가 숫자로 나왔다.
문턱은 그 사이인 **0.15**.

### 10.2 글자가 통째로 **굵어지고 있었다**

HWP 는 「〈 조 건 〉」 같은 머리글을 칠하지 않고 **윤곽만 얇게 긋는다**(PDF 텍스트
렌더 모드 stroke). MuPDF 는 그것을

```
<use href="#font_9_86" stroke-width=".1668" transform="matrix(9,0,0,-9,…)"/>
```

로 적는데, SVG 에서 `<use>` 의 `stroke-width` 는 **참조된 내용의 좌표계**에서
해석되므로 그 변환(9배)에 **같이 곱해진다** — 0.1668pt 로 그으려던 것이 1.5pt 가 된다.

⚠️ **겹쳐 대조로는 못 잡았다.** 그 그림의 평균 절대차는 0.0250 으로 문턱(0.05)
아래였다. **400dpi 로 확대해 눈으로 보고서야** 드러났다. 고치니 0.0092.

---

## 11. RPM 자리 600 — 계획 3개로 되찾기 (완료)

### 11.1 세 계획이 어긋나는가 (전량 교차)

| | 값 |
| --- | --- |
| gated ∩ main667 | 403 |
| gated ∩ recrop1646 | 317 |
| main667 ∩ recrop1646 | 459 |
| 겹치는 문항의 **rect 불일치** | **0 / 545** |
| page 불일치 | 49 — 전부 3-2. gated 는 `page=origin+3`, recrop·main 은 origin 쪽 |

rect 는 세 파일이 같은 값을 들고 있고, 그 값은 `rpm-origin.json` 의
`source_coords` 와 **같다**(이미 자리를 구한 1,010장도 계획 rect × dpi ≠ PNG 픽셀
0건). 계획이 주는 것은 그림 칸이 아니라 **어느 책·어느 쪽·어느 상자인가** 다.

자리 못 구한 600 / 600 이 recrop1646 에 있다. 무리 그림 93 중 idx 0 은 36장.

### 11.2 왜 `figure_rect` 재현이 600장을 못 찾았나

원장 재현은 `PAGE_OFFSETS = (0, 3)` 과 문항 상자 안에서 `figure_rect` 를 다시
불렀다. 표본 20장을 다시 재니:

- 쪽 오프셋 **−3** 이 필요한 장이 있다(1-1·1-2). `(0, +3)` 만 보면 그 쪽을
  아예 안 본다. 실패한 5장 중 3장이 이것.
- 상자 안에서 찾으면 **라벨·옆 그림 조각**을 집는다. 같은 5장을 쪽 전체에서
  찾으니 겹쳐 대조 0.044~0.090 → **0.004~0.026** 으로 떨어졌고, 나란히 놓고
  보니 디스크 PNG 와 같은 그림이다.

방법: 계획/원본이 가리키는 쪽 후보 `{origin−3, origin, origin+3, gated.page}`
각각을 72dpi 로 한 장 그려 디스크 PNG 를 찾고, 파일 dpi 로 ±1pt 다듬는다.
문턱은 원장과 같은 **0.03**. 크기는 PNG 픽셀 ÷ dpi 라 교차검산이 구조적으로
맞는다.

`scripts/figure/fill-rpm-rects-from-plans.py` 전량 결과 (1,163초):

| | 장수 |
| --- | ---: |
| 대상 (자리 없던 RPM) | 693 (600 + 무리 93) |
| **되찾음** | **631** (600 중 547 · 무리 93 중 84) |
| 못 찾음 | 62 (계획으로도 53 · 무리 9) |
| 그중 벡터 | +584 → 원장 벡터 1,921 → **2,505** |

통과 겹쳐 대조: 최소 0.0022 · 중앙 0.0108 · 90% 0.0190.

### 11.3 🔴 숫자가 초록이어도 옆 문항이었다 — 눈 7장

쪽 전체 검색 + 문턱 0.03 은 638장을 통과시켰다. 「문제 없음」을 차이 큰 순으로
보니 `019fd1d7-a1ec` 가 **디스크는 △OBC, 찾은 칸은 △DEF**. 평균차 0.0295.
같은 부류 6장이 더 있었다(다른 도수분포표, `191226` 숫자, 다른 요일표).

96×96 평균차는 닮은 도형을 구조적으로 못 가른다. 열쇠를 바꿨다: **문항
상자(+80pt)와 안 겹치고 차이가 0.020을 넘으면 버린다.** 0.020 이하 36장은
실린더·교점·상자그림으로 눈이 맞아서 남겼다. 7장을 되돌렸다 → 631.

대조표: `.work/rpm-rect-fill-sheet.png`(되찾은 앞 16 · 맞음)
`.work/rpm-far-1.png` · `rpm-far-2.png`(상자 밖 43 · 여기 7장의 거짓이 보임).

---

## 12. SVG 전량 재실행 + 눈 대조 (2026-08-19 이어받기)

판정자 고친 뒤(`INK_LOSS_REJECT = 0.15`, `<use>` 굵기) `extract-vector-svg.py` 를
그대로 돌렸다. 1,651초. 원장 벡터 행이 1,921 → **2,505**(§11 에서 RPM 584장이
더해짐).

| | 장수 |
| --- | ---: |
| 돌린 행 | 2,505 (원장 벡터+rect+원본있음 전량) |
| **통과 → `public/figures-svg`** | **2,467** (기출 888 · RPM 1,579) |
| 버림 | 38 (기출 29 · RPM 9) |
| SVG 합계 | 116.1MB · 중앙 39.5KB · 최대 1,699KB |

겹쳐 대조: 중앙 0.0147 · 75% 0.0215 · 90% 0.0304 · 최대 0.1977.
문턱 diff 0.05 · ink_loss 0.15.

옛 실행(문턱 ink_loss 0.3)은 통과 1,659 · 버림 262. 버림 242장이 번짐 오탐이었다는
§10.1 과 맞는다 — 고친 자는 버림 38만 남겼다.

### 12.1 눈으로 본 것 — 「문제 없음」을 차이 작은 순으로

대조표는 `.work/svg-eye/` (커밋 안 함). 왼쪽 디스크 PNG, 오른쪽 Chromium SVG.

| 표 | 본 것 |
| --- | --- |
| 통과 차이 최소 12 | 점 패턴·보기 상자·점 A/B/E. 글자 내용이 같다. ℃↔¾ 류 없음. |
| 통과 차이 최대 8 | 식 상자·표는 같다. 원뿔 A/B/C 는 **채색이 연해진다**(C 띠가 하얗게). 도형·라벨은 같다. |
| 통과 획손실 최대 8 | 보기 상자. 내용 동일. 가는 테두리 번짐이 획손실 0.14~0.15 로 잡힌 것. |
| 통과 RPM 고루 10 | 표·각·사다리꼴·산점도. `일교차(℃)` 도 양쪽에 ℃. 맞음. |
| 통과 기출 고루 10 | 보기 상자·확률표. 기출 벡터가 표·상자라는 §9 와 눈으로도 같다. |
| 버림 38 (work PNG) | 기출 보기 상자는 **내용이 같은데** 가는 테두리 때문에 획손실 0.15+ 로 버린 것이 많다. RPM 9장 중 `019fd1de-65d4`(diff 0.1977) 는 왼쪽 40°·20°, 오른쪽 30° — **다른 그림. 버린 것이 맞다.** 원탁에 사람 6명이 오른쪽에서 빠진 것도 버림이 맞다. |

`<use>` 굵기: 산출 SVG 에 `stroke-width="0.0151185"` × `matrix(11.03,…)` ≈ 0.167pt.
고치기 전(0.1668×9≈1.5pt)이 아니다. `2038/q20.svg` 「조건」 머리글이 그 모양.

### 12.2 `<img>` 로 띄우기

`check-svg-as-img.mjs` 첫 실행은 60/60 실패. 원인은 SVG 가 아니라 **검사 쪽
`file://`** — Chromium 이 about:blank 에서 로컬 파일을 안 연다. data URL 로
바꾸니 **80/80 정상.** `naturalWidth` 265px = 70mm@96dpi. 지면이 http 로
`/figures-svg/…` 를 물리면 같은 경로다.

### 12.3 못 한 것 (이 세션)

- 버린 38장 중 내용이 같은 기출 보기 상자를 되살리지는 않았다. 판정은 버리는 쪽.
- 원뿔 채색이 연해지는 부류는 문턱 아래라 통과했다. 몇 건인지는 **못 셌다.**
- 실물 인쇄 검수는 안 했다(절대 규칙 6). `src/` 를 안 건드렸다.
- SVG 116MB 를 git 에 넣을지 — 넣는다. `public/figures` 와 같은 자리의 산출이다.
