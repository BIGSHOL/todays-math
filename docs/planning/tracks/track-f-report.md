# 트랙 F 보고 — F-1 재현 · F-2 전량 대조 · F-3 드라이런 (2026-08-16)

> 브리프: `track-f-newload.md` · 근거 수치: `08-import-ledger.md` §1.1~§1.3
> **F-3 에서 멈췄다. 공유 DB 에는 한 줄도 쓰지 않았다.** 적재(F-4)는 승인 후.

## 0. 한 줄 결론

원장 표는 **한 칸도 안 틀리고 재현됐다**(2022년 버킷 ±1 제외). 그런데 같은 데이터의
**본문을 실제로 보니 그대로 넣으면 안 되는 것이 294행** 있었고, `externalId` 로는
안 잡히는 **편 통째 중복이 4편** 있었다. 둘을 빼고 **5,816행 / 300편**이 적재 대상이다.

| | 행 |
|---|---|
| 원장 §1.3 "즉시 적재 가능" | 6,174 |
| − 본문 결함 (아래 §3) | −294 |
| − 편 단위 중복 (아래 §2) | −64 |
| **적재 대상** | **5,816** |

---

## 1. F-1 재현 — 원장 §1.3 은 전부 맞다

`npx tsx scripts/qa/load-survey.ts` · 읽기 전용

| 구분 | 편 | 실체 | 소단원 있음 | 매핑 성공 | 그중 정답 |
|---|---|---|---|---|---|
| DB 적재됨 | 1,193 | 23,118 | 23,099 (99.9%) | 22,133 | 16,979 |
| 미적재·PDF 있음 | 1,034 | 19,805 | 234 (1.2%) | 159 | 122 |
| 미적재·PDF 없음 | **556** | 11,140 | **6,174** (55.4%) | **6,015** | **4,582** |
| 미적재·2022년 | 161 | 3,116 | 0 | 0 | 0 |
| 추출물 없음 | 6 | — | — | — | — |
| **적재 대상 합계** | **1,590** | **30,945** | **6,408** | **6,174** | **4,704** |

§1.3 표의 **모든 칸이 일치**한다. 편수 넷도 정확히 맞는다.
추출물 없는 6편은 원장 §5.1.2 의 "본문(PDF)만 6" 과 같은 무리다(HWP 원본이 없다).

### 1.1 다른 칸 하나 — 2022년 버킷 실체 3,116 vs 원장 3,115

**숫자를 고치지 않았다.** 원인을 못 찾았다. 확인한 것:

- 연도 분포는 깨끗하다 — 161편 전부 `year=2022`, null 없음. 버킷 경계 문제가 아니다.
- 길이 경계(`>` vs `>=`)가 아니다 — 40자 정확히인 문항이 네 버킷 모두에 있어
  부등호를 바꾸면 나머지 세 버킷도 같이 틀어진다.
- 공백 처리 차이가 아니다 — JS `trim` · 파이썬 `strip`(`\x1c-\x1f`·`\x85` 포함) ·
  NFC 정규화 셋 다 **3,116** 으로 같다.

이 버킷은 제외 규칙 대상이고 소단원 보유가 0% 라 어떤 하류 숫자에도 안 쓰인다.
**원장 표를 고칠지 여부는 코디네이터 판단.**

### 1.2 ⭐ "실체 문항" 정의가 트랙 D 와 다르다 — 이걸 모르면 표가 안 맞는다

원장 §1.1 각주는 "본문 40자 이상" 이라고만 적혀 있는데, **재는 대상이 `stem` 원문 하나**다.
트랙 D `audit-missing-questions.ts` 는 같은 40자를 `buildHwpContent`(지문+보기)에 잰다.

| 정의 | 적재됨 | PDF있음 | PDF없음 | 2022 |
|---|---|---|---|---|
| **`stem` 원문 ≥40** (원장) | **23,118** ✓ | **19,805** ✓ | **11,140** ✓ | 3,116 |
| `buildHwpContent` ≥40 (트랙 D) | 25,111 | 21,601 | 11,632 | 3,353 |

4~9% 차이다. `load-survey.ts` 가 두 값을 다 낸다(`실체_트랙D정의`).
**원장 §1.1 각주에 "지문 원문 기준" 을 명시하는 게 좋겠다.**

### 1.3 학년 힌트가 안 잡히는 조합 (브리프 §5 가 세라고 한 것)

| 조합 | 편 | 실체 문항 |
|---|---|---|
| 고2/null | 4 | 74 |
| 고1/수학 | 1 | 21 |
| 고1/null | 1 | 21 |

적재 대상 5,816행 중 **25행**이 학년 미해석 상태로 전 학년 풀에서 단원을 골랐다.
눈으로 확인함 — **초등 단원에 실린 행 0**, 힌트가 `조건부확률`·`이항분포`처럼
고유한 이름이라 전부 `확률과 통계` 로 제대로 붙었다.
학년이 해석된 행 중 **배정 단원의 학년이 힌트와 다른 행도 0**이다.

부수 확인: `final-pairs.json` 메타 ↔ `hwp-latex` 메타 불일치 **0건**.

---

## 2. F-2 전량 대조 — `externalId` 만 봤으면 4편을 통째로 중복 삽입했다

`npx tsx scripts/qa/load-dedupe-check.ts` · DB 36,597행 전량 · 읽기 전용

열쇠를 넷 썼다.

| 열쇠 | 겹친 후보 | 판단 |
|---|---|---|
| `externalId` (원장 §2.2) | **0** | — |
| 본문 해시 (원장 §2.1) | **3** | 전부 아래 4편 안에 있다 |
| 한글 서명 (`sigKo`) | 235행 | **행으로는 못 가른다** — 아래 참조 |
| 원본 파일명 | **0** | — |

### 2.1 한글 서명은 **행이 아니라 편으로** 봐야 갈라진다

행 단위 235건은 진짜 중복과 문구 우연 일치가 섞여 있다. 학교가 다른
`삼각형 ABC에서 … 외접원의 반지름의 길이는?` 류가 숫자만 다른 채 같은 서명을 갖기 때문이다.
전체 본문 유사도로도 안 갈라진다 — 기존 DB 행은 PDF 텍스트 레이어라 **같은 문항인데
서식이 달라** 0.85 근처로 떨어지고, 거기에 템플릿 형제가 겹친다.

**편 단위 겹침률로 보면 깨끗하게 갈라진다.**

| 겹침률 | 편 | 실체 |
|---|---|---|
| 50~64% | **4** | 진짜 중복 |
| 20~50% | **0** | (비어 있다) |
| ≤12% | 125 | 문구 우연 일치 (겹친 행 130) |

가운데가 비어 있어 기준값(0.3)을 어느 쪽으로 움직여도 판정이 안 바뀐다.

### 2.2 걸린 4편 — 같은 시험지가 다른 `examId` 로 이미 있다

원본 경로까지 확인했다. 학교·학년·회차·출판사가 같고 **과목 표기만
`수하`↔`공수2`(= `HIGH_SUBJECT` 별칭)** 로 다르다.

| 후보 | 원본 | DB 편 | DB 원본 |
|---|---|---|---|
| 2916 효성여고 | `기출작업\이전차수\175차\[효성여고][1][수하][24-2-중간][천재이]` | 2915 | `HWP 2 PDF\...\[효성여고][1][공수2][24-2-중간][천재이]` |
| 2875 중앙고 | `...\165차\[중앙고][1][수하][24-2-기말][미래앤]` | 4499 | `...\[중앙고][1][공수2][25-2-기말대비][동아]` |
| 2845 운암고 | `...\174차\[운암고][1][수하][24-2-중간][미래엔]` | 4473 | `...\[운암고][1][공수2][25-2-중간대비][미래엔]` |
| 2659 경북여고 | `...\169차\[경북여고][1][수하][24-2-중간][미래엔]` | 4230 | `...\[경북여고][1][공수2][25-2-중간대비][미래엔]` |

**64행 전량을 뺐다.** 겹치는 행만 빼면 한 시험지가 두 `examId` 로 쪼개져 DB 에 남는다 —
그건 편 단위 결손(F)이 아니라 문항 단위 결손(트랙 D, 같은 `examId` 안)의 층이다.
원장 §1.2 가 섞지 말라고 한 그 경계다.

### 2.3 후보 집합 안의 자기 중복

`externalId` 0 · 본문 해시 0 · 한글 서명 63묶음.
63묶음은 **전부 학교가 다른 템플릿 형제**다(정확도 80%↔90%, 계수만 다름).
같은 편 안의 반복은 1건뿐. **편 단위 자기 중복은 없다.** 그대로 둔다.

---

## 3. ⚠️ 지표는 초록인데 실물이 틀렸다 — 본문 결함 294행

F-1 표가 한 칸도 안 틀리고 맞았기 때문에 **여기서 멈췄으면 그대로 적재했을 것이다.**
표본을 눈으로 보다가 발견했다.

| 결함 | 행 | 편 | 처리 |
|---|---|---|---|
| **base64 덩어리** | **239** | 15 | 뺀다 |
| 지면 머리말 | 35 | — | 뺀다 |
| PUA 잔재 | 11 | — | 뺀다 |
| 보기 중복 | 9 | 9 | 뺀다 |
| 워터마크 | 186 | — | **지운다** |
| 작업자 서명 | 1 | — | **지운다** |

지우기와 빼기를 가른 기준은 하나다 — **경계를 확실히 자를 수 있는가.**

### 3.1 base64 덩어리 239행 (15편, 전부 2025년)

지문이 이렇게 나온다:

```
닫힌구간 7HmWZvKIj4Fgqftip7R/ReOniozAZ/0ZBqj25K0OMOyHN76QV8r9ZB4fB26C+hxTQCuBUnCFQxJQvGYpJnk8GXmkCOruG/gZIw== $\le
```

100자 단위로 끊긴 인코딩 덩어리다. 그대로 넣으면 학생 시험지에 난수가 찍힌다.
`hwp-latex` 3,302편 전량으로는 **310문항 / 18편**이고, 12편은 통째로, 3편은 1~2행만 오염됐다.
편으로 뭉개지 않고 **행 단위로** 뺐다.

⚠️ **원본 쪽 문제로 보인다.** `hwp/`(원본 한글 수식본)에도 똑같이 있고,
같은 편의 기존 DB 행은 `sourceFile` 이 **PDF** 인데도 오염돼 있다. 추출기를 고치기 전에
원본 문서를 봐야 한다.

### 3.2 지면 머리말 35행 — 사람 이름이 시험지에 찍힌다

```
2025년 1학기 기말고사 / 사인법칙 ~ 수학적 귀납법 / 경덕여고 2학년 수학1 / 학원로고 / 강민구
```

다섯 줄 덩어리라 경계를 못 자른다. 트랙 D 도 같은 이유로 지우는 대신 막았다(H12).

### 3.3 워터마크 186행 — 트랙 D 규칙이 **줄 단위라 놓친다**

`stripWatermark` 는 줄 전체가 워터마크일 때만 지운다. 그런데 정규분포표처럼 표가 있는
문항에서는 셀에 붙어 나온다:

```
$\mathrm{P}(0\leq Z\leq z)$대구광역시 내신 수학 연구회
```

문구가 한 종류뿐이라는 건 트랙 D 가 이미 확인했으므로 **줄 가운데 것도 지웠다**.
작업자 서명(`오검:권 보선t`)도 같은 이유로 놓치고 있었다 — 트랙 D 규칙이 이름을
`\S{0,20}` 로 봐서 **이름에 공백이 있으면 안 잡힌다**.

> 트랙 D 소유 파일은 고치지 않았다. 트랙 F 쪽 `sanitizeContent` 에서만 처리했다.
> **트랙 D 도 같은 구멍을 갖고 있다** — 교체한 4,069행에 같은 워터마크가 남아 있는지
> 확인이 필요하다(코디네이터 판단).

### 3.4 보기 중복 9행 — 기존 DB 보다 17배 깨끗하다

`30 / 32 / 34 / 34 / 34`, `ㄱ / ㄱ / ㄱ,ㄷ / ㄴ,ㄷ / ㄱ,ㄴ,ㄷ` 같은 것들이다.
원본 `hwp-latex` 부터 이 상태라 조립 과정의 문제가 아니다.

| | 보기 4개 이상 | 보기 중복 |
|---|---|---|
| 트랙 F 후보 | 4,388 | **9 (0.2%)** |
| 기존 DB `past_exam` | 24,249 | **846 (3.5%)** |

---

## 4. F-3 드라이런 — 넣을 것

`npx tsx scripts/qa/load-dry-run.ts` · **`--apply` 가 아예 없다**

```
대상 1,590편 → 후보 5,880행(결함 제외 후) → 편중복 −64 → 적재 5,816행 / 300편
```

`toLoadRows` 에서 추가로 걸러지는 행 **0** · D-37 위반 **0** · 본문 10,000자 초과 **0**.

| 항목 | 값 |
|---|---|
| 정답 보유 | 4,414 (75.9%) · 없음 1,402 |
| 소단원 | **144개** (전체 735개 중) |
| 학교 | 138곳 |
| 연도 | 2025년 5,197 · 2024년 619 |
| 학기·회차 | 1-기말 3,809 · 2-기말 809 · 2-중간 694 · 1-중간 504 |
| 출제 형식 | 객관식 4,379 · 서술형 1,093 · 단답형 343 · 미상 1 |
| 난이도 | mid 3,138 · easy 1,946 · hard 732 |
| 본문 길이 | 평균 228자 · 최대 1,409자 |

학년 분포 — 확률과 통계 2,004 · 미적분1 1,044 · 대수 890 · 중2 784 · 중3 537 ·
중1 265 · 공통수학1 256 · 공통수학2 18 · 기하 18.

> ⚠️ **소단원 144개에 몰려 있다.** 상위 단원이 `중2 일차함수의 그래프와 절편` 281행,
> `확률과 통계 조건부확률` 202행이다. 전체 735개 소단원 중 19%에만 붙는다 —
> 나머지는 소단원 없는 24,537문항 쪽에 있다(원장 §5.1.4, 손대지 않았다).

### 4.1 트랙 A 인계

`scripts/qa/reports/load-figure-handoff.json` — **1,010행 / 297편 · 그림 1,176장**.
`figureUrls` 는 비운 채로 넣는다(브리프 §6-2).

⚠️ 본문 `[그림]` 자리표시로는 못 센다 — 그건 옛 PDF 파이프라인 표기이고
트랙 D HWP 추출본에는 **0건**이다. 트랙 D `hwpx-figures.json`(편→문항→장수)을 썼다.

---

## 5. 코디네이터 판단이 필요한 것

1. **적재 승인** — 5,816행. 조건과 근거가 적힌 메시지로 주면 F-4 를 만든다.
   멱등: `externalId` UNIQUE + `selectMissingLoadRows`. 두 번 돌려도 신규 0.
2. **보기가 전부 그림인 객관식 7행** (`5201-13 5357-9 5401-14 5418-14 5499-1 5766-11 5865-12`).
   본문 보기가 `1. 2. 3. 4. 5.` 로 비어 있어 트랙 A 가 그림을 붙이기 전에는 출제 불가다.
   브리프 §6-2 대로 **넣는 쪽으로 두었다.** 빼려면 알려 달라.
3. **`Problem.questionType` 을 신규 행에 채웠다** (객관식/단답형/서술형).
   트랙 E 소유 컬럼이지만 **기존 행은 안 건드리고 새 행에만** 넣는다 — 안 넣으면
   트랙 E 가 5,816행을 다시 백필해야 하고, 그 코퍼스에 이 편들은 없다. 반대면 뺀다.
4. **원장 §1.1 각주 보강** — "실체 문항 = 지문 원문 40자" 로 명시 (§1.2 참조).
5. **원장 §1.1 의 2022년 3,115 → 3,116** 여부 (§1.1 참조).

## 6. 다른 트랙에 넘기는 것 (F 는 손대지 않았다)

- **트랙 D** — ① `stripWatermark` 가 줄 가운데 워터마크와 공백 있는 작업자 서명을
  놓친다(§3.3). 교체한 4,069행 점검 필요. ② `audit-missing-questions.ts` 의 실체 정의가
  원장 §1.1 과 다르다(§1.2).
- **트랙 A** — `load-figure-handoff.json` (§4.1).
- **기존 DB 결함 (누구 것도 아님, 원장에 기록 필요)**
  - base64 오염 **49행 / 3편**(5577 운암중 · 5283 성화중 · 5630 학남중).
    **트랙 D 교체분이 아니다** — 교체 대상 10행의 교체 *이전* 본문이 이미 오염돼 있었고,
    백업 4,069행 중 교체 전 오염은 그 10행뿐이다. `sourceFile` 이 PDF 라 옛 파이프라인 유입분이다.
  - 보기 중복 **846행 (3.5%)** (§3.4).

## 7. 산출물

| 파일 | 내용 |
|---|---|
| `scripts/qa/load-survey.ts` | F-1 재현 (읽기 전용) |
| `scripts/qa/load-candidates.ts` | 후보 생성 공용 — 위생 규칙이 여기 있다 |
| `scripts/qa/load-dedupe-check.ts` | F-2 전량 대조 → `load-exclusions.json` |
| `scripts/qa/load-dry-run.ts` | F-3 드라이런 → `load-rows.json` (F-4 가 읽는다) |
| `src/__tests__/unit/loadCandidates.test.ts` | 위생 규칙 15건 — **픽스처 전부 실데이터** |

트랙 D 산출물 경로는 `TRACK_D_REPORTS` 로 바꿀 수 있다(기본값은 트랙 D 워크트리).
`buildHwpContent`·`sigKo` 는 베끼지 않고 트랙 D 원본을 그대로 부른다 — 판정이 갈라지면
재현이 재현이 아니게 된다.
