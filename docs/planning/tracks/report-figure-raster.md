# 그림 래스터 트랙 보고서

> 워크트리 `그림화질` · 브랜치 `feat/figure-quality`
> 지시서: `docs/planning/tracks/brief-figure-raster.md`
> 작성 시작: 2026-08-19. **작업 도중에 계속 갱신한다.**

---

## ㉮ 비용 5항목

원장님 지시: 「① 비용부터 재라」. 아래에 없는 숫자는 아직 못 잰 것이다.

### 1. 분모 — 원본이 몇 편이고, 디스크에 있는가

**무엇을 보고 셌는가**

- 디스크: `public/figures/` 의 png/jpg/jpeg/gif/webp (2026-08-19 이 워크트리)
- 대장: `scripts/figure/figure-manifest.json`
- 원본 경로: `F:\시험지변환기\db\exam_index.db` 의 `exams.src_path`
- 존재 여부: `Path.exists()` 로 **그 경로를 직접** 봤다. 인벤토리·대장만 보고 「있다」고 하지 않았다.

| | 수 | 근거 |
| --- | ---: | --- |
| 디스크 그림 | **16,104장** · 307.7 MB | `public/figures` 전수. 지시서 §1 의 16,122장보다 **18장 적다** — 그 18장이 어디로 갔는지는 못 좇았다 |
| 기출 편 / 장 | **2,208편 / 14,401장** | 디렉터리 이름이 숫자인 것 |
| RPM 장 | **1,703장** | `public/figures/rpm/` (지시서 1,721보다 18장 적음 — 위 18장과 같은 수) |
| 기출 대장 | 1,896편 · 경로 12,921 | `figure-manifest.json` |
| 기출 편 중 대장이 있는 것 | 1,896편 | 전부 `src_path` 가 **`.pdf`** |
| 기출 편 중 대장이 없는 것 | **312편** | 아래 표 |

대장에 없는 312편 (그림 996장):

| src_path 확장자 | 편 | 디스크 파일 이름 |
| --- | ---: | --- |
| `.hwp` | 305 | `hwp-*` 986장 |
| `.hwpx` | 1 | `hwp-*` |
| `.pdf` | 6 | `pdf-q*` 5장 + `hwppdf-q*` 4장 |

이 312편은 `extract-all-figures.py` 가 만든 것이 아니다. HWP 회수 경로(`hwp-qNN.png`)다. **한컴 COM 을 띄우지 않기로 했으므로 이 996장은 이번 재크롭 대상이 아니다.**

원본 파일 존재 (그림 있는 기출 2,208편):

| | 편 | 그림 |
| --- | ---: | ---: |
| `src_path` 가 적혀 있고 디스크에 **있다** | **2,208** | **14,401** |
| `src_path` 가 적혀 있으나 파일 없음 | 0 | 0 |
| `src_path` 자체가 없음 | 0 | 0 |
| `db/pages/<id>/src.pdf` 로컬 캐시 | **0** | — `F:\시험지변환기\db\pages` 디렉터리 자체가 없다 |

확장자 (그림 있는 기출): `.pdf` 1,902 · `.hwp` 305 · `.hwpx` 1.

재크롭에 **지금 당장** 쓸 수 있는 원본:

| 무리 | 편/권 | 그림 | 원본 |
| --- | ---: | ---: | --- |
| 기출 · 대장 있는 PDF | 1,896편 | 대장 경로 12,921 (디스크 기출 14,401 중 이쪽) | N드라이브 `src_path` PDF, **1,896편 전부 존재** |
| 기출 · HWP/HWPX 회수분 | 306편 | 986장 | 원본은 있으나 **한컴 COM 금지**라 다시 못 자른다 |
| 기출 · 대장 없는 PDF 6편 | 6편 | `pdf-q*`/`hwppdf-q*` | PDF 는 있다. 파일 이름 규약이 달라 `extract-all-figures` 그대로는 안 덮는다 |
| RPM | 6권 | 1,703장 | `N:\개인\강아\교재자료\RPM\22\RPM 중학 *-학생용.pdf` **6권 전부 존재**. 이 워크트리에 `.rpm-src/` 와 `rpm-crop-plan.json` 은 **없다** |

exam_index 전체 5,925편도 `src_path` 가 전부 디스크에 있었다. 다만 그 5,925 안에는 그림 없는 편·2020 HWP 가 섞여 있다. **분모는 그림이 있는 2,208편이다.**

### 2. 시간 — 편 10개 실측 × N

**무엇을 돌렸는가.** 대장 있는 PDF 1,896편을 번호순으로 고르게 10편 골랐다
(728, 2493, 2991, 3277, 3614, 3852, 4087, 4373, 4942, 5532).
추출기는 `extract-all-figures.py` 그대로(`--dpi 300 --png --out public/figures-300`).
기존 `figure-manifest.json` 과 `public/figures` 는 안 덮었다.

| 측정 | 실측 | 근거 |
| --- | ---: | --- |
| 10편 `map_exam` (첫 열기) | **4.696s** | 편당 0.16~0.90s |
| 10편 추출 (같은 PDF, 직후) | **3.551s** | 추출기 stdout 「3s」. 방금 연 PDF 라 **캐시됐을 수 있다** |
| 100편 `map_exam` (균등) | **42.6s** | 실패 0. 편당 0.426s |

외삽 (선형, 「N편 실측 ×」):

- 전량 1,896편 **map만**: `100편 실측 42.6s × 18.96 = **808s**` (약 13.5분)
- 전량 1,896편 **추출**: 추출 = map + 쓰기. 10편 추출 3.6s 는 캐시 뒤라 **하한**이다.
  100편 map 42.6s 가 더 정직한 바닥. 쓰기는 10편에서 map 보다 크지 않았다.
  → **전량 추출을 15분 안쪽으로 본다. 캐시 안 된 1,896편 추출 실측은 아직 없다.**

RPM 1,703장은 이 시계에 **안 넣었다.** 계획은 다시 만드는 중이라 아직 못 돌렸다.

### 3. 디스크 — 300dpi 전량 / 지금 `public/figures`

지금 용량 (전수, 이 워크트리):

| 무리 | 지금 |
| --- | ---: |
| 기출 · 대장 있는 PDF | **209.1 MB** |
| 기출 · 대장 없는 (HWP 회수) | 75.3 MB |
| RPM | 23.3 MB |
| 합 | 307.7 MB |

10편 실측 (57장, 추출기 그대로 300dpi PNG):

| | 지금 | 300dpi PNG | 배수 |
| --- | ---: | ---: | ---: |
| 10편 합 | 0.82 MB (860,434 B) | 4.49 MB (4,704,009 B) | **×5.467** |

외삽: `10편 실측 ×5.467` → 대장 있는 기출 209.1 MB × 5.467 ≒ **1,143 MB**.
**HWP 75.3 MB 와 RPM 23.3 MB 는 이 배수에 넣지 않는다.**

⚠️ 이 배수의 정체: 같은 57장에서 가로 픽셀이 늘어난 것은 **6장뿐**(폴백).
나머지 51장은 가로가 그대로다(네이티브). 용량이 다섯 배가 된 주원인은
**JPEG → PNG** 이지 300dpi 가 아니다. 100편 네이티브 비율(아래)이 10편과
다르므로 전량 배수도 이 숫자가 아닐 수 있다 — **100편 디스크는 못 쟀다.**

### 4. 네이티브 vs 폴백

확장자로 나누지 않았다. `map_exam()` 의 `xref` 있음/없음이다.

| 표본 | 편 | 그림 | 네이티브 | 폴백 | 네이티브 % |
| --- | ---: | ---: | ---: | ---: | ---: |
| 번호순 균등 10편 | 10 | 57 | 51 | 6 | 89.5% |
| 번호순 균등 100편 | 100 | 599 | 498 | 101 | **83.1%** |
| 1,896편 전량 | — | — | — | — | **못 쟀다** |

100편은 43편에 폴백이 하나라도 있었고, 57편은 폴백 0.

⚠️ **폴백 ≠ 도형.** 폴백이 많은 편을 눈으로 열었다 (아래 ㉰).
2180(폴백 9)·1994·3538 의 PNG 는 거의 전부 `<보기>`·`<조건>`·`<자료>` **글상자**다.
진짜 벡터 도형 폴백은 10편 안에서 월암중 3614 의 소인수분해 나무(q02) 정도였다.
83.1% 네이티브는 「다시 잘라도 안 는 그림」의 **상한**이고, 300dpi 가 돕는
도형의 비율은 그 나머지 16.9%보다 **작다.** 글상자를 뺀 폴백 비율은 못 셌다.

`extract-all-figures.py` 는 `클립 렌더` 를 올리기만 하고 안 찍고 있었고,
`xref 못찾음` 은 **올리는 줄 자체가 없었다.** `--dpi`/`--manifest`/`--png` 를
열었고, 마지막에 `네이티브`·`클립 렌더` 를 찍게 했다. 기본 경로·기본 DPI 는
그대로다.

### 5. 못 잰 것 · 아직 안 한 것

- 지시서 16,122장과 지금 16,104장의 **18장 차이** — 파일 단위로 못 좇았다.
- 1,896편 **전량** 네이티브 비율 · **전량** 추출 시간 · **100편** 디스크 배수.
- HWP/HWPX 306편 996장 — 원본은 있으나 한컴 COM 없이 다시 못 자른다.
  `.hwp-pdf/` 캐시도 이 워크트리에 없다.
- RPM 1,703장 재크롭 시간·디스크 — `rpm-crop-plan.json` 이 이 워크트리에 없다.
  원본 6권은 `N:\개인\강아\교재자료\RPM\22\` 에 있다. 계획은 아직 안 만들었다.
- `figure-rect-ledger.json` (벡터 세션) — 이 워크트리에 아직 없다.
- 재크롭 전량 뒤의 `measure-figure-resolution.py` 전수. 아래 표는 **같은 10편**뿐이다.

---

## 같은 자 전후 (10편만)

자: `scripts/qa/measure-figure-resolution.py` 의 `effective_dpi` (지면 폭 상한 70mm).
새 지표를 만들지 않았다.

| 지면 실효 DPI | 지금 10편 | 300dpi PNG 10편 |
| --- | ---: | ---: |
| 96 (작아서 확대 안 됨) | 7 (12.3%) | 6 (10.5%) |
| 100~150 (거칠다) | 6 (10.5%) | 6 (10.5%) |
| 150~200 | 16 (28.1%) | 17 (29.8%) |
| 200~300 | 12 (21.1%) | 9 (15.8%) |
| 300 이상 | 16 (28.1%) | 19 (33.3%) |
| **150 미만** | **13 (22.8%)** | **12 (21.1%)** |
| 가로 px | 120 · 중앙 545 · 1620 | 132 · 중앙 549 · 1620 |

이름 짝 57 · 가로 늘음 **6** · 같음 **51** · 줄음 0.

이 10편의 지금 150 미만(22.8%)은 전수 48.1%보다 낮다. **이 10편은 전수의
대표가 아니다.** 「150 미만이 한 자릿수」는 이 표본만으로 말할 수 없다.
네이티브가 천장인 한, 전량 재크롭을 해도 150 미만은 한 자릿수로 안 떨어질
가능성이 크다 — 전량 자를 때까지는 그렇게 **단정하지 않는다.**

가로가 늘어난 6장 (폴백 · 200→300dpi):

| 파일 | 지금 | 300 | 눈 |
| --- | --- | --- | --- |
| 728/q09 · q18 · q19 | 664px / 241dpi | 996px / 361dpi | **보기 상자**. 글자가 조금 또렷. 도형 아님 |
| 3614/q02 | 366px / 133dpi | 549px / 199dpi | **소인수분해 나무.** 선·숫자 30/15 가 분명 또렷. 이게 재크롭이 돕는 부류 |
| 3614/q02_1 | 242px / 96dpi | 363px / 132dpi | 같은 나무의 빈 칸. 선이 또렷 |
| 4942/q14 | 120px / 96dpi | 179px / 96dpi | **(가)(나) 빈칸.** 그림이 아님. 작아서 여전히 96dpi |

---

## ㉰ 눈으로 본 것 (비용 단계에서 이미 보인 것)

숫자를 통과한 뒤 나란히 열었다. 「문제 없음」쪽(네이티브, 가로 불변)을 먼저 의심했다.

1. **728/q04 (네이티브 JPEG, 355px, 129dpi)** — 좌표평면. 옛 JPEG 와 새 PNG 가
   **같은 픽셀**이다. 점 이름 A~E 가 흐리고 격자선이 깨진다. 300dpi 재크롭이
   할 수 있는 일이 없다. §5 잡음 감소 후보.
2. **728/q13 (네이티브, 234px, 96dpi)** — 수 타일(−2.4, a, 0.25…). 스캔 사진.
   역시 그대로. 작아서 지면에서도 작다.
3. **3852/q01 (네이티브 JPEG)** — 직각삼각형 ABCD. 선이 이미 깨끗하다.
   옛·새 차이가 눈에 안 띈다. 「문제 없음」으로 보이는 네이티브.
4. **3852/q12 (네이티브, 227px, 96dpi)** — 빨간/회색 삼각형. 작아서 선이 각진다.
   다시 잘라도 안 는다.
5. **2180/q01 (네이티브 JPEG)** — 전개도(10cm, 9cm). **사진의 특징**이 그대로다
   (원장님이 보신 그 부류). 폴백이 아니라 네이티브라 재크롭 대상이 아니다.
6. **2180 · 1994 · 3538 의 PNG** — 폴백 상위 편을 열었더니 `<보기>` `<조건>`
   `<자료>` 글상자였다. 3828 등 나머지 폴백 상위는 **아직 안 열었다.**
7. **RPM `019fd1d6-f4e6` (200dpi 렌더)** — y=a/x 와 삼각형 OAP. 선이 부드럽다
   (벡터를 200 으로 래스터화한 흔적). 좌표 경로는 아직 안 돌렸다.
   이 파일은 예전에 「발문·선택지가 들어왔다」고 적힌 근거 문항이다.
   지금 디스크 본은 그래프만 있다. 그때와 같은 파일인지는 **확인하지 않았다.**

반대쪽: 「멀쩡한 네이티브에 300dpi 재크롭을 대면 픽셀이 늘면 안 된다」
→ 51/57 가로 불변. 늘어난 6장은 전부 폴백. 이 검사는 통과.

---

## 진행

- [x] 분모 (항목 1)
- [x] 10편 실측 · 시간 외삽 (항목 2) — 전량 추출 실측은 아직
- [x] 디스크 배수 표본 (항목 3) — 10편만
- [x] 네이티브/폴백 100편 (항목 4) — 전량은 아직
- [ ] 300dpi 재크롭 전량 (기출 PDF 1,896 + RPM)
- [ ] 같은 자 전수 전후 표
- [ ] 잡음 감소 + 4가드
- [ ] 눈으로 더 보기 (폴백 상위 나머지 · RPM · 개선 전후)
