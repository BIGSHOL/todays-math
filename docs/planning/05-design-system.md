# Design System (기초 디자인 시스템) — 오늘의수학

> ⚠️ **이 문서는 협의용 초안입니다.**
> 디자인은 원장님이 직접 깊이 관여하기로 결정했습니다 (D-07).
> 아래 내용 중 `[협의 필요]` 표시가 있는 항목은 **제안일 뿐, 확정이 아닙니다.**
> 구현 전 원장님과 화면 단위로 함께 결정합니다. AI 코딩 파트너는 이 문서의 미확정
> 항목을 임의로 확정해서 구현하지 않습니다.

---

## MVP 캡슐

| # | 항목 | 내용 |
|---|------|------|
| 1 | 목표 | 진도만 입력하면 수학 일일/확인테스트가 5분 안에 완성되어, 매일 30분~1시간을 되찾는다 |
| 2 | 페르소나 | 반별·학생별 진도가 다른 동네 수학학원 원장/강사 |
| 3 | 핵심 기능 | FEAT-1: 진도 기반 자동 출제 |
| 4 | 성공 지표 (노스스타) | 주 5일 이상 실제 사용 |
| 5 | 입력 지표 | ① 진도 입력→인쇄까지 5분 이내 ② 무수정 사용률 80% |
| 6 | 비기능 요구 | 수식이 깨지지 않는 A4 인쇄 품질 |
| 7 | Out-of-scope | 학생용 앱, 자동 채점, 모바일 앱, 결제 |
| 8 | Top 리스크 | AI 생성 문제 품질로 인한 검수 부담 |
| 9 | 완화/실험 | 검수 화면 1클릭 교체 + 무수정 사용률 측정 |
| 10 | 다음 단계 | /tasks-generator로 TASKS.md 생성 → Phase 0 시작 |

---

## 0. 디자인 작업 방식 (확정)

이 프로젝트의 디자인 협업 규칙 — **이 섹션만은 확정 사항입니다:**

0. **목표는 회피가 아니라 정체성이다 (2026-08-13 원장님 지시).** "정형화된 AI 생성 툴처럼
   보이지 않는 것"은 최소 조건일 뿐, 진짜 목표는 **오늘의수학만의 특색 있는 틀** — 첫눈에
   구별되는 비주얼 정체성이다. 이를 위해 **첫 UI 구현(T1.2) 전에 '디자인 정체성 세션'을
   선행**한다: 서로 방향이 다른 시안 2~3안(실제 화면 목업)을 제시 → 원장님이 방향 선택 →
   그 방향의 문법(색/타이포/형태/밀도)을 이 문서에 확정 기록 → 이후 모든 화면이 그 문법을
   따른다. 참고 후보: 원장님이 직접 설계한 eywa의 "Editorial 7문법"
   (`C:\Creative\eywa\docs\design-conventions.md` — de-mono, 폰트 두께 2종 제한, 알약 통일,
   WCAG 대비 치트시트)은 원장님 취향의 실증 사례로서 방향 후보 중 하나가 될 수 있다.
1. **AI 공장식 스타일 전면 배제.** 다음은 금지 목록입니다:
   - 보라/파랑 그라데이션 히어로, 유리morphism 카드
   - shadcn/ui 등 기성 컴포넌트 킷의 기본 모양 그대로 사용
   - 목적 없는 이모지·아이콘 남발
   - 화면마다 반복되는 동일한 카드 그리드 레이아웃
   - "AI가 만든 것 같은" 뻔한 인상을 주는 모든 관성적 선택
2. **컴포넌트는 Tailwind CSS로 직접 제작** (D-15). 기성 킷 미사용.
3. **결정 프로세스 — Wire → Hi-fi 2단계, 각 4~5안 (2026-08-13 원장님 지시, 확정)**:
   - ① **Wire 단계**: 구조/레이아웃 와이어프레임 **4~5안** 제시 → 원장님 선택
   - ② **Hi-fi 단계**: 선택된 구조 위에 시각 디테일(색/타이포/형태/질감) **4~5안** 제시 → 원장님 선택
   - ③ 확정 내용을 이 문서에 기록 후 구현 착수. **확정 전 구현 금지.**
   - ④ **세부 디테일(간격·문구·버튼 위치·아이콘 유무 등)은 항상 질문** — 사소해 보여도
     추정으로 확정하지 않는다.
4. **이 문서는 살아있는 문서**: 협의 결과가 나올 때마다 `[협의 필요]` → `[확정]`으로 갱신.

---

## 1. 디자인 철학

### 1.1 핵심 가치 (방향 합의됨, 세부는 협의)

| 가치 | 설명 | 구현 방법 |
|------|------|----------|
| 도구다움 | 감상하는 화면이 아니라 매일 쓰는 작업대 | 장식 최소화, 핵심 동선(검수→인쇄) 최단 클릭 |
| 차분한 신뢰감 | 토스처럼 정돈된, 요란하지 않은 인상 | 절제된 색 사용, 넉넉한 여백, 명확한 위계 |
| 손맛 있는 완성도 | 공장식 틀이 아닌, 이 서비스만의 디테일 | 직접 만든 컴포넌트, 원장님과 다듬은 세부 |

### 1.2 참고 서비스 (무드보드) `[협의 필요]`

| 서비스 | 참고할 점 (제안) | 참고하지 않을 점 |
|--------|----------|-----------------|
| 토스 | 정돈된 위계, 절제된 컬러, 명확한 다음 행동 | 모바일 중심 패턴 (우리는 PC 웹) |
| (원장님 추가 예정) | — | — |

> 원장님이 평소 "이건 보기 좋다"고 느낀 서비스/인쇄물을 공유해 주시면 무드보드에 반영합니다.

---

## 2. 컬러 팔레트 `[확정 — 2026-08-13 Hi-fi 세션, H5×G2]`

### 2.1 베이스 (뉴트럴 그레이 스케일 — H5 스위스 계기판)

| 역할 | Hex | 사용처 |
|------|-----|--------|
| **Ink (텍스트/룰)** | `#161616` | 주요 텍스트, 굵은 구분선(3px), 완료 전 주 버튼 |
| **Background** | `#ECECEA` | 앱 전체 배경 |
| **Surface (활성)** | `#FFFFFF` | 현재 처리 중(hot) 카드, 표 hover 행 |
| **Side Panel** | `#E0E0DE` | 우측 상주 패널 |
| **Divider** | `#C2C2C0` | 카드/행 사이 1px 구분선 |
| **Text Secondary** | `#6A6A68` | 메타 정보 |
| **Text Tertiary** | `#8A8A88` / `#A3A3A1` | 흐린 라벨, 미래 단계 |
| **Segment Empty** | `#CFCFCD` | 게이지 빈 칸 |

### 2.2 기능색 (구글 4색 — 색이 의미를 전달한다, G2)

| 색 | Hex | 의미 | 사용처 |
|------|-----|------|--------|
| **Blue** | `#1A73E8` | **지금 할 일** | 현재 단계 세그먼트/라벨, hot 카드 인셋 바(5px)·번호·주 버튼 |
| **Green** | `#34A853` | **끝난 단계** | 완료 세그먼트/라벨 (진행될수록 그린이 쌓임) |
| **Yellow** | `#FBBC05` (텍스트 `#C79102`) | **입력 대기** | 진도 미입력 반의 현재 세그먼트 |
| **Red** | `#EA4335` (텍스트 `#C5221F`) | **경고** | "남은 작업 N", 미입력 경고문, 오류 |

**운용 규칙**: 기능색은 위 의미로만 사용 — 장식 목적 사용 금지. 한 화면에 4색이 다 보여도
각각 의미가 달라 혼란이 없어야 한다. 의미 없는 곳은 전부 그레이 스케일.

### 2.3 다크 모드

- MVP에서는 라이트 모드만 지원 (인쇄 중심 도구 특성상 라이트 우선)
- 다크 모드는 v2에서 검토

---

## 3. 타이포그래피 `[협의 필요 — 아래는 출발점 제안]`

### 3.1 폰트 패밀리 (제안)

| 용도 | 제안 | 대안 | 상태 |
|------|------|------|------|
| 화면 본문 | Pretendard | system-ui, sans-serif | [협의 필요] |
| **시험지 인쇄** | 명조 — `KoPubBatang` / `Nanum Myeongjo` / `Noto Serif KR` (Mathgen `PAPER_FONTS.serifKR`) | 고딕(Pretendard)은 Hi-fi H3에서 탈락 | [확정 — 2026-08-14 T5.1. 실물 가독성은 T5.2 인쇄 검수] |
| 수식 | KaTeX 기본 (Computer Modern 계열) | — | 사실상 확정 (수식 표준) |

> **시험지 폰트는 화면 폰트보다 중요한 결정입니다.** 실제 인쇄물을 놓고 비교해서 정합니다.

### 3.2 타입 스케일 `[확정 — H5 최종안 기준]`

| 이름 | 크기 | 굵기 | 용도 |
|------|------|------|------|
| Logo | 19px | 900 | 마스트헤드 로고 (자간 -0.5px) |
| Class Name | 15px | 900 | 반 이름 |
| Big Number | 24~26px | 900 | 카드 순번, 통계 숫자 (tabular-nums) |
| Body | 12.5px | 700~900 | 버튼, 본문 강조 |
| Meta | 11.5px | 400 | 카드 메타 정보 (말줄임 처리) |
| Micro Label | 9~10.5px | 800 | 단계 라벨, 패널 제목 (자간 1.2~2.5px) |

**규칙**: ① 숫자는 항상 `font-variant-numeric: tabular-nums` ② 정보가 길면 줄바꿈 대신
말줄임(열맞춤 보호) ③ 굵기는 400/700/900 3단만 사용

---

## 4. 간격 토큰 (Spacing) — 관례적 기본값

| 이름 | 값 | 용도 |
|------|-----|------|
| xs | 4px | 아이콘-텍스트 간격 |
| sm | 8px | 요소 내부 여백 |
| md | 16px | 요소 간 간격 |
| lg | 24px | 섹션 간 간격 |
| xl | 32px | 큰 섹션 구분 |
| 2xl | 48px | 페이지 여백 |

> Tailwind 기본 스케일과 일치. 특별한 이견 없으면 이대로 사용하되, 화면 밀도 협의에 따라 조정.

---

## 5. 기본 컴포넌트 `[협의 필요 — 시안 단계에서 확정]`

### 5.1 버튼 (Button)

| 상태 | Primary | Secondary | Ghost |
|------|---------|-----------|-------|
| 기본 | 채움 + 밝은 텍스트 | 테두리 + 컬러 텍스트 | 텍스트만 |
| 호버 | 약간 어둡게 | 배경 옅게 채움 | 밑줄 |
| 포커스 | 포커스 링 | 포커스 링 | 포커스 링 |
| 비활성 | 낮은 투명도 | 낮은 투명도 | 낮은 투명도 |
| 로딩 | 스피너 + 문구 유지 | 동일 | 동일 |

**문구 톤 (D-08 확정)**: 간결하고 사무적. "출제 완료", "인쇄하기", "진도 저장" — 느낌표·이모지 없음.

### 5.2 입력 필드 (Input)

| 상태 | 스타일 |
|------|--------|
| 기본 | 테두리 + 배경 |
| 포커스 | Primary 테두리 + 포커스 링 |
| 에러 | Error 테두리 + 간결한 에러 문구 |
| 비활성 | 회색 배경, 입력 불가 |

### 5.3 핵심 화면별 특수 컴포넌트 (이 서비스만의 것 — 함께 설계)

| 컴포넌트 | 화면 | 설계 포인트 | 상태 |
|----------|------|------------|------|
| 진도 트리 선택기 | S-07 | 학년>대단원>소단원 트리에서 1클릭 진행 (10초 입력 목표) | [협의 필요] |
| 문제 카드 (검수용) | S-05 | 수식 렌더링 + 난이도/유형 표시 + [교체] 버튼 위치 | [협의 필요] |
| 시험지 미리보기 | S-06 | A4 지면 그대로 보여주기 — 화면과 인쇄의 일치가 생명 | [확정 — 지면 §8.5와 동일. 화면 크롬은 T5.2] |
| 시험지 지면 디자인 | 인쇄물 | 학원명/날짜/이름칸/문항 배치 — **가장 중요한 디자인 작업** | [확정 — 2026-08-14 T5.1, §8.5] |

> **시험지 지면이 이 서비스의 진짜 얼굴입니다.** 학생과 학부모가 매일 보는 결과물이므로,
> 화면 UI보다 먼저 원장님과 지면 디자인을 확정하는 것을 권장합니다.

---

## 6. 접근성 체크리스트

### 6.1 필수 (MVP)

- [ ] **색상 대비**: 텍스트와 배경의 대비율 4.5:1 이상
- [ ] **포커스 링**: 키보드 탐색 시 포커스 표시 명확
- [ ] **클릭 영역**: 버튼/링크 최소 44x44px
- [ ] **에러 표시**: 색상만으로 구분하지 않음 (텍스트 병행)
- [ ] **폰트 크기**: 화면 본문 최소 14px, 시험지는 인쇄 가독성 별도 검증

### 6.2 권장 (v2)

- [ ] 키보드 전체 탐색 가능
- [ ] 스크린 리더 호환 (ARIA 라벨)

---

## 7. 아이콘

| 옵션 | 설명 | 상태 |
|------|------|------|
| Lucide | 깔끔한 라인 아이콘 — 필요한 곳에만 절제 사용 | [협의 필요] |

**사용 규칙 (제안)**: 아이콘은 의미 전달에 필요한 곳에만. 장식용 아이콘 금지 (섹션 0 원칙).

---

## 8. 확정 기록 (협의 완료 항목)

### 8.1 S-03 메인 화면 구조 `[확정 — 2026-08-13 Wire 세션]`

Wire 5안(A~E) 중 A(오늘 스택)+E(대장부 표) 결합안으로 확정:

- **자동 전환**: 오늘 처리할 일이 남아 있으면 **A 스택 상태**, 전부 완료되면 **E 대장부 표 상태**로 자동 전환
- **우측 진도 입력 패널 상주** (두 상태 모두) — 수업 직후 10초 입력 + 이번 주 통계
- 세부 장치 3종 채택: ① ⇄ 수동 전환 토글 ② 스택 하단 완료 반 한 줄 요약 ③ 표에 "내일 테스트" 열 (자동 준비 예약 표시)
- 시각 스타일(색·타이포·질감)은 Hi-fi 세션에서 별도 확정

### 8.2 수업 카드 단계 표시기 `[확정 — 2026-08-13 원장님 지시]`

- 모든 수업 카드는 **동일 위치에 4단계 파이프라인 표시기**를 갖는다: `진도 → 출제 → 검수 → 인쇄`
- 단계가 진행될수록 **같은 자리의 라벨이 전진**한다 (완료 단계는 채움, 현재 단계는 강조, 미래 단계는 흐림) — 카드들을 훑으면 각 반이 어디까지 왔는지 진행감이 느껴져야 함
- **주 버튼은 항상 현재 단계의 동사 하나**: "진도 입력" / "검수" / "인쇄" — 단계가 넘어가면 버튼도 바뀐다
- 단계 표시기의 시각 표현(도장/체크/세그먼트 등)은 선택된 Hi-fi 방향의 문법을 따른다

### 8.3 시각 방향 `[확정 — 2026-08-13 Hi-fi 세션]`

- **H5 "스위스 계기판"** 채택: 뉴트럴 그레이 스케일 + 단일 강조색, 강한 그리드, 큰 순번 숫자,
  tabular 숫자, 직각, 장식 0. 단계 표시기는 4칸 세그먼트 게이지
- **열맞춤 필수 (원장님 지시)**: 카드 내부는 고정 컬럼 그리드 — 번호·반 정보·단계 게이지·주 버튼이
  모든 카드에서 **동일한 x좌표**에 정렬. 정보 길이가 달라도 컬럼이 밀리지 않는다 (줄바꿈 대신 말줄임)
- **깔끔하고 컴팩트한 것이 최우선 (원장님 지시)**: 여백·장식보다 정보 밀도와 정렬. 요소 추가 시
  "이게 컴팩트함을 해치는가"를 먼저 물을 것
- 세부 토큰(강조색 확정값, 타이포 스케일, 간격)은 H5 최종안 검수 후 §2~4에 기록

### 8.4 색 운용 `[확정 — 2026-08-13]` + 카드 그리드 스펙

- **G2 구글 4색 기능 배분** 채택 (§2.2) — 블루=할 일 / 그린=완료 / 옐로=대기 / 레드=경고
- **카드 고정 그리드**: `44px(번호) | minmax(0,1fr)(반 정보) | 232px(게이지) | 112px(주 버튼)`
  — 완료 요약 줄 포함 모든 행 동일. gap 16px, 카드 패딩 12px 24px
- 단계 게이지: 4칸 세그먼트, 높이 8px, 칸 간격 3px. 표 안 미니 게이지는 높이 7px 폭 110px
- hot 카드: 흰 배경 + 좌측 블루 인셋 5px. 대기 카드: opacity 0.62~0.78
- **참조 시안 (구현 시 시각 기준)**: `docs/design/mockups/hifi-h5-final.html`(레이아웃·열맞춤),
  `docs/design/mockups/hifi-h5-google.html`의 **G2**(색 운용), `docs/design/mockups/wire-s03-final.html`(구조·상태 전환)

### 8.5 시험지 지면 `[확정 — 2026-08-14 T5.1]`

화면 UI(H5 스위스 계기판)와 시험지 지면은 목적이 다르므로 문법을 맞추지 않는다.
지면은 학생·학부모가 매일 보는 얼굴이다.

**베이스**: Mathgen `JaseupTemplate` (자습). Hi-fi는 H1 원본 금색.

| 항목 | 확정 |
|------|------|
| 종이 | 따뜻한 흰 `#FCFCF8` (`paperWarm`) |
| 액센트 | 금색 `#A57F00` |
| 본문 폰트 | 명조 (`PAPER_FONTS.serifKR`). 라벨·학원명은 sans (`Pretendard`) |
| 수식 | KaTeX Computer Modern |
| 영문 라벨 | 유지 (`SELF-STUDY`, `SCRATCH PAD`) |
| 학원명 | 좌측 상단 작은 금색 「오늘의수학」 (`SELF-STUDY` 위) |
| 학습일 | 우측 상단. 값은 테스트 예정일 |
| 이름칸 | 학습일 아래 한 줄 `이름 ________ · 반 ______` |
| 제목 | `{테스트유형} · {소단원}` 예: `일일테스트 · 일차부등식` |
| 오늘의 목표 | 제목 아래 한 줄. 단원/출제 메타에서 채움 |
| 핵심 개념 박스 | **일일·확인 모두 첫 장에 항상**. 금색 좌측 5px 바 |
| 문항 번호 | `문 N` (금색 명조 18px) |
| 배점 | **없음**. 일일/확인테스트는 점수가 아니라 맞은 개수 / 전체 개수 |
| 내 정답 · 채점 | 문항마다 밑줄 + 체크 칸 |
| 풀이 | 우측 16px 모눈 (`SCRATCH PAD`). 1단 그리드 `1.15fr / 1fr` |
| 밀도 | 장당 2문항. 8문항 일일 = 문제지 4장 (풀이 공간 우선) |
| 이어지는 장 | 간소 헤더 `오늘의수학 · {제목}` + `p. N`. 개념 박스 없음 |
| 하단 | `오늘의 메모` 점선 + `p. N` |
| 여백 | Mathgen 자습 그대로 (첫 장 padding 36/50, 본문 padX 50) |
| 정답지 | 같은 금색 자습 얼굴. 빠른 정답 그리드 + 2단 해설. 번호는 `문 N` |

**이식 범위 (T5.2)**: `JaseupTemplate` + 공통 `BodyContainer`/`ProblemBody`/`printPack` + 정답지.
헤더에 학원명·이름/반 한 줄을 얹는다. 배점(`PointsLabel`)은 이 지면에서 그리지 않는다.
나머지 5템플릿(평가원/정통/모던/워크북/유형)은 가져오지 않는다.

**참조 시안**:
- 확정 합성: `docs/design/mockups/print-t51-jaseup-final.html`
- 베이스 6안: `docs/design/mockups/print-t51-templates.html`
- Hi-fi 5안: `docs/design/mockups/print-t51-jaseup-hifi.html`

**T5.2로 넘기는 실물 검수**: 명조 가독성, 모눈 농도, 여백, 수식 깨짐, 4장 스테이플.

## Decision Log 참조 (디자인 관련)

| ID | 항목 | 선택 | 근거 |
|----|------|------|------|
| D-07 | 디자인 방향 | 토스 느낌 + AI 공장식 배제 + **사용자 주도 협업** | 사용자 명시 요구 |
| D-08 | 톤 | 간결·사무적 | 매일 쓰는 도구답게 |
| D-15 | 스타일링 | Tailwind CSS, 컴포넌트 직접 제작 | 기성 킷의 획일적 인상 회피 |
| D-23 | 디자인 프로세스 | 화면별 제안→피드백→확정 후 구현. 확정 전 구현 금지 | 디자인 오너십은 원장님에게 |
| D-24 | 최우선 디자인 작업 | 시험지 지면 (인쇄물) | 학생·학부모가 매일 보는 서비스의 얼굴 |
| D-28 | 시험지 지면 | 자습 H1(금색 명조) + 학원명/이름칸. 배점 없음(맞은 개수/전체) | 2026-08-14 T5.1 원장님 확정 |
