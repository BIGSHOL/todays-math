# 16. 그림 유실·회수 원장

> **이 문서는 「지금 몇 건이 그림 없이 남아 있나」를 언제든 확인하는 자리다.**
> 숫자를 손으로 고치지 말 것 — 아래 §1 명령을 돌리면 현재값이 나온다.
> 회수를 한 번 돌릴 때마다 §3 에 한 줄 추가한다.

작성 2026-08-18 · 원장님 지시(「회수분에 대해 문서로 남겨서 항상 체크가능하도록」)

---

## 0. 무엇이 문제인가

본문이 「오른쪽 그림과 같은 평행사변형 `ABCD` 에서…」라고 지목하는데
`figureUrls` 도 `figureSvg` 도 비어 있으면, 그 문항은 지면에 **그림 없이** 나간다.
학생은 못 푼다. 단원이 맞든 틀리든 상관없다.

발견 경위: 단원 판정 보류 2건을 끝내다가 그중 한 건(「∠x의 크기를 구하시오」)이
**단원 문제가 아니라 그림 유실**임이 드러났다. 전수로 세니 1,420건이었고
**1,372건이 그때 출제 가능한 상태**였다.

---

## 1. 지금 상태 확인 — 이 명령 하나

```bash
npx tsx scripts/qa/report-missing-figures.ts          # 집계
npx tsx scripts/qa/report-missing-figures.ts --list   # 대상 전량
npx tsx scripts/qa/report-missing-figures.ts --json   # scripts/qa/reports/missing-figures.json
```

판정 규칙은 `scripts/qa/missingFigureRule.ts` **하나뿐**이다.
세는 쪽과 잠그는 쪽이 같은 파일을 읽는다 — 목록을 양쪽에 각각 쓰면 둘이 같이 눈이 먼다
(CLAUDE.md 2026-08-18).

### 출력에 `미분류` 가 0이 아니면

**목록이 아직 눈멀었다는 뜻이다.** 그 표본을 눈으로 보고 규칙에 반영하거나
오탐 목록에 넣어라. 조용히 넘기지 말 것 — 첫 지시어 목록 5개는 198건을 못 봤고,
그 사실은 「그림」 전수와 대조해서야 드러났다.

---

## 2. 현재값 (2026-08-18 적대적 리뷰 반영 후)

| 구분 | 건수 | 비고 |
|------|-----:|------|
| **그림 유실** | **857** | 본문이 그림을 지목하는데 그림이 없다 |
| └ 그중 지금 출제 가능 | **0** | 전량 `directUseAllowed=false` 로 잠금 |
| └ RPM 교재본(`transformed`) | 681 | 교재 PDF 4권 필요 + 오려내기 신뢰도 미달 — §4.1 |
| └ 기출(`past_exam`) | 174 | 사유별 분해는 §4.2 |
| └ 자작(`manual`) | 2 | 그림 없이는 못 푸는 자작 문항 |
| 본문 오염 (`[그림]` 자국) | 35 | 유실이 아니라 **머리말·학원 로고가 딸려 온 자국**. 별개 결함 — §4.3 |
| 그림이 필요 없는 것 | 31 | 그림그래프·「상자그림에서」·「…으로 나타내시오」 등, 표본 확인 완료 |
| 미분류 | 20 | **판정 못 함이지 «문제 없음»이 아니다.** 「가로등 그림자」류와 전개도 개념 문항이다. 잠그지 않으므로 멀쩡한 문항이 조용히 빠지지 않는다 |

> 791 → 857 로 **늘었다.** 적대적 리뷰가 규칙의 사각지대를 찾아내 열쇠를 넓힌 결과다
> (§3.4). 숫자가 준 것이 아니라 **안 보이던 것이 보이게 된 것**이다.

시작점은 **1,420건 / 출제 가능 1,372건**이었다.

---

## 3. 회수 이력 — 돌릴 때마다 한 줄 추가

| 날짜 | 무엇을 | 회수 | 남은 유실 | 명령 |
|------|--------|-----:|---------:|------|
| 2026-08-18 | 대장에 이미 그림이 있던 문항 되붙이기 | **42** | 1,378 | `reconnect-figures.mjs --apply` |
| 2026-08-18 | 그림 유실 전량 **출제 제외 잠금** | — | 1,378 (출제 가능 0) | `apply-missing-figure-lock.ts` |
| 2026-08-18 | 2차 인계분 346편 중 PDF 있는 **306편** N드라이브 추출 → 되붙이기 | **548** | 830 | 아래 §3.1 |
| 2026-08-18 | 잔여 4편 추가 추출 시도 | 0 | 830 | 추출기가 그림 0장 — 검출 실패이거나 원본에 없음 |
| 2026-08-18 | **RPM 교재 PDF 에서 좌표대로 오려내기** (1-1·2-1 두 권만 확보) | **37** | 793 | §4.1 |
| 2026-08-18 | 오탐 2건 해제 — 「상자그림에서 …무엇을 나타내는가」는 지면의 그림이 아니다 | (해제 2) | 791 | `apply-missing-figure-lock.ts --revert --reclassified` |
| 2026-08-18 | **적대적 리뷰 반영** — RPM 37건 **되돌림**, 규칙 재설계로 66건 추가 잠금 | (−37, +66 잠금) | 857 | §3.4 |
| 2026-08-18 | **보기 그림 짝** — 97건 회수(드라이런) · 43건 출제 제외(드라이런) | 계획만 | 857 | §5 |

누적 **590건 회수**(기출 745 → 174). RPM 은 회수분을 **되돌려** 681로 돌아왔다 — 아래 §3.4.

> ⚠️ **규칙을 고쳤으면 잠금도 같이 풀어야 한다.** 오탐을 잠근 채 두면 멀쩡한 문항이
> 조용히 출제에서 빠진다. `--revert --reclassified` 가 그 일을 한다 —
> 원장에 있는 것 중 **지금 규칙으로 더는 「유실」이 아닌 것**만 골라 푼다.

### 3.1 2차 인계분 회수 절차 (재현용)

원인은 단순했다 — `scripts/qa/handoff/load2-figure-handoff.json`(346편/1,021행)이
**트랙 A 에 전달만 되고 처리되지 않았다.** 본문은 2026-08-17 에 적재됐는데
마지막 그림 커밋은 2026-08-16 이다.

```bash
# 1. 인계 편 목록으로 match-dir 을 만든다 (편별 키 = examId)
#    → scripts/qa/reports/_load2-match/<examId>.json

# 2. N드라이브 원본 PDF 에서 직접 추출 (db/pages 캐시는 이 컴퓨터에 없다)
python scripts/figure/extract-all-figures.py --from-pairs \
       --match-dir scripts/qa/reports/_load2-match

# 3. DB 에 되붙이기 (그림 언급이 있고 파일이 실제로 있는 것만)
ALLOW_SHARED_IMPORT=1 node scripts/figure/reconnect-figures.mjs --apply

# 4. 회수된 것만 출제 풀에 되돌린다
ALLOW_UNIT_FIX=1 npx tsx scripts/qa/apply-missing-figure-lock.ts --revert --recovered
```

실측: 306편 → **3,344장 · 52.4 MB · 2,891문항**, 그중 되붙인 것 548건.
소요 177초. LLM 토큰 0.

⚠️ **4번을 빠뜨리면 회수분이 잠긴 채 남는다.** 회수와 해제는 한 세트다.

### 3.2 육안 검수한 것

숫자를 믿기 전에 표본을 봤다. 되붙인 그림이 **그 문항의 그림인지** 직접 확인:

- `2928-17` 반지름 2인 구를 중심에서 거리 1인 평면으로 자름 → 그림에 「1」「2」 표기 ✓
- `2928-3` 높이 6인 그릇의 수면 → 그림에 「6m」 ✓
- `1236-9` `y=√(ax−b)+3ab` 그래프 ✓ · `1318-8` `y=(ax+b)/(x+c)` 점근선 x=2·y=−1 ✓

그리고 **조각을 하나 잡았다** — `2065-4` 는 15장인데 첫 장이 「y축과 빗금 하나」였다.
벡터로 그려진 도형을 추출기가 획 뭉치별로 쪼갠 것이다. 붙였으면 지면에 파편 15개가
나갔다. `reconnect-figures.mjs` 에 **`MAX_SHEETS = 6` 가드**를 넣었고(스키마
`figureUrls` 주석의 실측 최대치), 막힌 것은 리포트의 `장수과다_조각의심` 에 남는다.

> 선택지마다 그림인 문항은 **6장이 정상**이다(`1987-10` 물병 1 + 선택지 그래프 5).
> 장수만 보고 자르면 멀쩡한 것을 버린다 — 그래서 6까지는 통과시킨다.

### 3.3 ⚠️ 남아 있는 정밀도 한계 — **옆 문항 그림이 딸려 온다**

되붙인 `1236-9`(무리함수 그래프)를 열어 보니 **2장**이 붙어 있었다. 첫 장은 맞는데
둘째 장이 격자점 그림이었다 — **다음 문항의 그림**이다. 매퍼(`map-figures.py`)가
그림을 「바로 앞 문항 번호」에 붙이므로, 뒤 문항의 번호 앵커를 못 잡으면 앞 문항으로
넘어온다. `10-handoff.md` §8.6-2 가 이미 예고한 한계다.

**실측 (2026-08-18, 기출 전체):**

| | 건수 |
|---|---:|
| 기출 중 그림 보유 | 7,851 |
| └ **2장 이상** | 557 |
| └ 이번 회수로 새로 생긴 다장 | 78 (549 중, 나머지 471은 1장) |

즉 **대부분(471/549)은 1장이라 안전**하고, 다장은 이전 회차에도 있던 기존 문제다.

**고치지 않은 이유 — 자동 판정이 안 선다.** 「선택지가 글자면 그림은 1장이어야 한다」로
걸러 보니 241건이 걸렸는데, 표본을 보니 **오탐이 섞여 있었다**:

- `3710-6` 「점 I 는 △ABC 의 내심이고, 점 O 는 △DEF 의 외심이다」 → **두 삼각형 그림 둘 다 필요**
- `3514-6` `<보기>` 규칙 그림 + 계산할 식 그림 → 둘 다 필요
- 반면 `3093-14`·`3408-12` 는 도함수 그래프 하나면 되는데 2장이다

**장수만으로는 못 가른다.** 그림을 눈으로 보고 「이게 이 문항의 그림인가」를 물어야 한다.
자동으로 둘째 장을 자르면 `3710-6` 같은 멀쩡한 문항이 반쪽이 된다.

**필요한 것**: 검수 화면에서 사람이 **그림을 옆 문항으로 옮기는 기능**
(`10-handoff.md` §8.6-2 가 요청한 그것). 그때까지는 다장 문항이 지면에
남의 그림을 하나 더 달고 나갈 수 있다 — **틀린 그림은 아니고 남는 그림이다.**

⚠️ **이 항목은 인쇄물에 영향을 준다. CLAUDE.md 절대 규칙 6에 따라 원장님 실물 출력
검수가 필요하다** — 특히 다장 문항이 지면에서 어떻게 앉는지.

### 3.4 ⚠️ 적대적 리뷰가 뒤집은 것 (2026-08-18)

세 갈래로 리뷰를 돌렸고, **두 갈래가 진짜 결함을 찾았다.** 여기 적는 이유는
같은 함정을 다시 밟지 않기 위해서다.

#### (1) RPM 오려내기 37건 중 14건이 결함 — **되돌렸다**

리뷰가 37장을 **전부 다시 오려 커밋된 것과 대조하고 눈으로 봤다.** 결과:
발문 통째 3 · 쪽 장식 1 · 그림 일부 잘림 5 · 축 라벨 잘림 5.

가장 아픈 것은 `019fd1d6-f4e6` 다 — **오려내기 주석이 「이 결함을 고쳤다」는 근거로
직접 인용한 바로 그 문항**인데, 커밋된 이미지에는 발문·선택지·쪽 배지가 다 들어 있었다.
내가 규칙을 고친 뒤 **표본을 다시 봤지만 하필 그 문항을 안 봤다.**

> **교훈: 규칙을 고쳤으면 «고치기 전에 확인한 것»부터 다시 봐야 한다.**
> 새 표본만 보면 회귀를 못 본다. 이 세션에서만 두 번 그랬다.

원인 셋을 고쳤다(`crop-rpm-from-pdf.py` 주석에 근거와 함께):
`min(t.width, band.width)` → `t.width` (발문 줄을 그림 띠에 견주던 자리) ·
크기 비율 → **으뜸에서의 거리**(쪽 장식이 으뜸의 65%나 됐다) · 박스 밖 조각 처리.

**그래도 다시 붙이지 않았다.** 고친 뒤에도 「그림이 박스 밖까지 뻗은」 부류가 남고,
「검증했다」고 한 뒤 두 번 회귀한 파이프라인이다. 재부착 조건은 §4.1 에 적었다.

#### (2) 판정 규칙이 한 단계라 **예외가 지시어를 늘 이겼다**

「**다음 그림과 같이** … 나무의 **그림자**가」가 `그림자` 예외에 걸려 그림 없이
출제 가능 상태로 살아 있었다. 결정적 검사는 이것이다 —
**그림이 붙어 있는 행 9,070건에 같은 규칙을 대 보면 「불필요」가 나오면 안 된다.**
22건이 나왔다. 그 자체가 반증이다. 지금은 1건이다.

그리고 질의 열쇠가 「그림」 한 낱말이라 **「전개도를 접어서 만든 정육면체」를 구조적으로
못 봤다**(살아 있던 것 전개도 15 · 산점도 20). 열쇠에 전개도·산점도를 넣었다.

#### (3) 잠금이 **이 컴퓨터에서만 되돌릴 수 있었다**

원장 파일이 `.gitignore` 에 걸려 커밋되지 않았다 — 공유 DB 를 790건 바꿔 놓고
되돌리기는 로컬 디스크에만 있었다. `scripts/qa/reports/` **디렉터리 자체**를 무시하면
안쪽 `!` 예외가 먹지 않는다(기존 `topic-coverage.json` 예외도 실제로는 안 먹고 있었다).
`/*` 로 바꿔 고쳤다. 함께 고친 것: **원장을 DB 보다 먼저 쓴다**(중간에 죽으면 undo 가
사라진다) · `--reclassified` 가 **그림 유무를 확인**한다(본문만 보면 그림 없는 행이 풀린다).

---

---

## 4. 남은 857건 — 왜 남았나, 무엇이 가능한가

### 4.1 RPM 교재본 681건 — 좌표는 다 있다. **막힌 곳이 둘이다.**

> 조사 완료 2026-08-18 (`scripts/qa/probe-rpm-figure-sources.ts`). **다시 조사하지 말 것.**

`diagram_assets` 경로는 `recover-rpm-figures.ts` 가 두 차례(`1e9e1fb1`, `b215515e`)
훑어 1,182문항/1,239장을 가져왔고 **정말로 고갈됐다** — 남은 671건에 대해 직접 세어
`diagram_assets` 행 **0**, `question_assets` **0**, `svg_path` 전부 NULL 을 확인했다.

**그런데 「고갈」은 그 테이블 얘기였다.** 원본에는 다른 길이 있었다:

| 원본에 있는 것 | 667건 중 |
|---|---:|
| `questions.source_file_id` | **667** |
| `questions.source_page_id` | **667** |
| `questions.source_coords` (crop bbox) | **667** |
| `source_files.storage_path` | **667** |
| `source_pages.image_path` (페이지 raster) | 0 |

좌표는 이런 모양이다 — **어느 페이지 어느 사각형인지가 그대로 적혀 있다**:

```json
{"x0": 56.67, "x1": 296.48, "y0": 635.94, "y1": 713.39, "page": 42}
```

필요한 원본은 **교재 6권뿐**이다:

| 원본 | 이 결함에 걸린 문항 | 상태 |
|---|---:|---|
| RPM 중학 2-2 학생용.pdf | 262 | ⛔ 없음 |
| RPM 중학 1-2 학생용.pdf | 208 | ⛔ 없음 |
| RPM 중학 3-2 학생용.pdf | 106 | ⛔ 없음 |
| RPM 중학 3-1 학생용.pdf | 43 | ⛔ 없음 |
| RPM 중학 2-1 학생용.pdf | 39 | ✅ PDF 확보 · 오려내기는 보류 |
| RPM 중학 1-1 학생용.pdf | 9 | ✅ PDF 확보 · 오려내기는 보류 |

#### 판은 **22개정**이다 — 쪽수로 확정했다

`source_files.storage_path` 가 `local:RPM 중학 1-1 학생용.pdf` 다. **`local:` 은 스토리지에
올라간 적이 없다는 뜻**이고(버킷 받기 6/6 HTTP 400, `byte_size` 도 0), 파일 자체는
sumaek 이 보관하지 않았다. 그래서 원본을 따로 구해야 한다.

N드라이브에 `개인/강아/교재자료/RPM/` 이 있다(`15/` 2015개정, `22/` 22개정, PDF 39개).

> ⚠️ **커밋된 인벤토리(`scripts/qa/nfile-inventory.txt.gz`, 21,009행)에는 이 폴더가 없다.**
> 인벤토리로 「없다」고 결론 내리면 틀린다. 실제 드라이브를 훑어야 나온다.

어느 판인지는 **쪽수로 갈렸다** — 이름이 비슷하다고 쓰면 좌표가 다른 쪽을 가리킨다:

| DB 기록 | 쪽수 | `15/`(2015개정) | `22/`(22개정) |
|---|---:|---:|---|
| RPM 중학 1-1 학생용 | 184 | `RPM 1-1` 176 ✗ | `22개정 RPM 중 1-1 학생용` **184 ✓** |
| RPM 중학 2-1 학생용 | 192 | `RPM 2-1` 168 ✗ | `22개정 RPM 2-1 학생용` **192 ✓** |
| RPM 중학 1-2 학생용 | 200 | `RPM 1-2` 176 ✗ | 없음 |
| RPM 중학 2-2 학생용 | 192 | `RPM 2-2` 160 ✗ | 없음 |
| RPM 중학 3-1 학생용 | 200 | `RPM 3-1` 160 ✗ | 없음 |
| RPM 중학 3-2 학생용 | 136 | `RPM 3-2` 120 ✗ | 없음 |

sha256 은 39개 전부 불일치했다(바이트가 다른 재저장본). 그래서 **쪽수가 맞는 것으로
실제로 오려 보고 눈으로 확인**했다 — 1-1 9건이 전부 그 문항의 그림이었다. 판이 맞다.

DB 의 sha256(원본과 바이트까지 같은지 확인용):

| 파일 | sha256 |
|---|---|
| 1-1 | `1a0500448ec1c852…` |
| 1-2 | `8109effe5b9dff90…` |
| 2-1 | `6233c4f9d9555aaf…` |
| 2-2 | `57e1f22d749b2bd1…` |
| 3-1 | `d1d16dcd99cae0c5…` |
| 3-2 | `31c926a09ff165f7…` |

#### ⛔ 막힌 곳 ① — 오려내기 신뢰도가 아직 모자란다

교재 1-1·2-1 로 37건을 오려 붙였다가 **적대적 리뷰가 14건을 결함으로 판정해 되돌렸다**
(§3.4). 원인 셋은 고쳤지만 「그림이 `source_coords` 밖까지 뻗은」 부류가 남는다.

**다시 붙이려면 이 조건을 먼저 채워야 한다:**

1. **자동 관문** — 오려낸 박스 안의 PDF 글자와 DB `content` 의 유사도를 재서 낮으면 버린다.
   리뷰가 1-1 로 실측해 중앙값 0.964 · 최저 0.923 이었다. 눈이 아니라 **숫자로** 판이
   맞는지 확인할 수 있고, 619건에도 그대로 쓴다.
2. **완비 검사** — 오려낸 영역이 그림 조각을 다 담았는지(박스 밖 조각을 안 버렸는지).
3. **`figureDims` 백필** — 지금은 붙여도 치수가 비어 넘침 판정이 기본값
   (264×207)으로 돌아간다(`printOverflow.ts`). `backfill-figure-dimensions.ts` 를
   회수 절차에 넣어야 한다.

#### ⛔ 막힌 곳 ② — **22개정 학생용 4권**

`22개정 RPM 중 1-2 / 2-2 / 3-1 / 3-2 학생용` (또는 같은 판의 다른 이름).
이 4권이 `.rpm-src/` 에 놓이면 **619문항**이 사람 판단 없이 붙는다.
쪽수가 각각 200 · 192 · 200 · 136 이면 맞는 판이다.

```bash
# 4권을 .rpm-src/ 에 놓고 (파일명은 DB 이름 그대로: `RPM 중학 1-2 학생용.pdf` 등)
npx tsx scripts/qa/recover-rpm-figures-from-pdf.ts           # 계획
python scripts/figure/crop-rpm-from-pdf.py                   # 오려내기
ALLOW_SHARED_IMPORT=1 npx tsx scripts/qa/recover-rpm-figures-from-pdf.ts --attach
ALLOW_UNIT_FIX=1 npx tsx scripts/qa/apply-missing-figure-lock.ts --revert --recovered
```

#### 오려내기가 쉬운 일이 아니었다 — 다섯 번 고쳤다

`source_coords` 는 **문항 전체**(발문 + 그림)라 그대로 오리면 발문이 지면에 두 번 나온다.
그림만 뽑는 과정에서 **눈으로 봐서** 다섯 가지를 잡았다. 전부 `crop-rpm-from-pdf.py` 주석에
근거와 함께 남겼다:

1. **쪽 전체가 이미지 블록 하나**라 문항 박스가 통째로 잡혔다 → 쪽 크기 요소는 뺀다.
2. 페이지 검출기(`map-figures.py`)의 **분모가 여기선 틀렸다** — 이미지가 두 문항에 걸치면
   「후보의 절반 이상이 이 문항 안」 규칙이 진짜를 버린다.
3. **수직선이 「긴 밑줄」로 걸러졌다** — 페이지 검출기가 얇고 긴 것을 밑줄로 본다.
4. **가로 군집이 성긴 그림을 쪼갰다** — 수직선 눈금 간격이 30pt라 점 하나만 남았다.
   그래서 거리로 가르되 **크기로** 남긴다(쪽 장식만 버린다). 「가장 큰 덩어리만」으로
   했을 때는 두 줄짜리 그림의 아랫줄을 잃었다.
5. **박스로 자르면 도형이 잘린다** — 정사각뿔 꼭대기가 박스 위로 28pt 나가 있었다.
   라벨도 세로만 보면 옆에 붙은 것을 잃는다(`3x-2y+12=0` 의 `=0` 이 사라졌다).

교훈은 늘 같다 — **표본을 눈으로 보기 전에는 초록이 초록이 아니다.**

#### 그 6권을 못 구하면

1. 도형 엔진 재작도(`F:\시험지변환기\core`) — 비용이 크고, 원본이 없으니 맞는지
   검증할 방법이 없다.
2. 폐기 — `12-discard-candidates.md` 에 이미 RPM 232건이 「그림 없음」으로 올라 있다.

### 4.2 기출 174건 — 사유별 분해

> 아래 표는 **155건이던 시점의 실측**이다. 규칙이 전개도·산점도까지 보게 되면서 19건이
> 늘었고, 늘어난 몫은 다시 재지 않았다. 재려면 §1 명령을 돌린 뒤 `--list` 로 대조한다.

| 사유 | 건수 | 회수 가능성 |
|------|-----:|------------|
| 편은 대장에 있는데 **이 문항만 없음** (검출 실패) | 62 | `python scripts/figure/diagnose-missing.py <examId>:<번호>` 로 진단 |
| **편 자체가 대장에 없음** | 86 | N드라이브에 PDF 가 있으면 `extract-all-figures.py --from-pairs` 로 회수 가능 |
| `examId`/문항번호가 없어 역추적 불가 | 7 | 2026-08-14 이전 이관분(`08-import-ledger.md` §3) — 불가 |

**「옆 문항에 붙은 것 아닌가」는 대체로 아니었다.** 62건 중 앞뒤 문항에 그림이 있는 것이
22건인데, 그중 **18건은 이웃이 그 그림을 정당하게 쓴다**(이웃 본문도 그림을 지목한다).
오배정이 의심되는 것은 **5건**뿐이다(`2065-4` `4338-19` `2394-19` `3635-17` `5325-19`).
즉 62건의 대부분은 진짜 검출 실패다 — 옆 문항에서 빼 올 그림이 없다.

### 4.3 본문 오염 35건 — 유실과 다른 결함

`[그림]` 자국 뒤에 학원 로고·머리말이 통째로 딸려 있다:

> `… 5. $2^{4}×3^{2}×5^{3}$ [그림] 내중 1학년 수학2025년 1학기 중간고사천내중 1학년 수학학원 로고…`

**잠그지 않았다.** 35건 중 `reviewStatus='approved'` 인 것이 **1건**뿐이라 실제로 지면에
나갈 수 있는 것은 그 1건이고, 나머지 34건은 이미 검수 대기·반려 상태로 걸러진다.
본문을 고치는 일(오염 제거)은 그림 회수와 다른 트랙이라 여기서 손대지 않았다.

⚠️ 다만 `[그림]` 이 **본문 한가운데**에 있고 그 자리에 실제 설명이 붙은 것도 있다
(「위 식의 다섯 개 연산 기호 아래에 각각 …」). 일괄 삭제하면 그 설명이 사라진다 —
**지우지 말고 사람이 보고 갈라야 한다**(CLAUDE.md 2026-08-16 「지운 게 증거였다」).

### 4.4 아직 손대지 않은 더 큰 후보

N드라이브에 PDF 가 있는데 아직 크롭을 돌리지 않은 시험지가 **약 935편** 있다.
이번엔 원장님 지시로 2차 인계분 306편만 돌렸다. 전량을 돌리면 이 결함 말고도
다른 문항의 그림이 함께 붙는다(이번 306편에서도 2,891문항이 붙었고 그중
되붙임 대상은 548건뿐이었다 — 나머지는 이미 그림이 있었다).

---

## 5. 잠금·해제 조작

```bash
npx tsx scripts/qa/apply-missing-figure-lock.ts                          # 드라이런
ALLOW_UNIT_FIX=1 npx tsx scripts/qa/apply-missing-figure-lock.ts         # 잠근다
ALLOW_UNIT_FIX=1 npx tsx scripts/qa/apply-missing-figure-lock.ts --revert --recovered      # 그림이 붙은 것만 해제
ALLOW_UNIT_FIX=1 npx tsx scripts/qa/apply-missing-figure-lock.ts --revert --reclassified   # 규칙이 바뀌어 유실이 아니게 된 것만 해제
ALLOW_UNIT_FIX=1 npx tsx scripts/qa/apply-missing-figure-lock.ts --revert                  # 전량 해제
```

원장: `scripts/qa/reports/missing-figure-lock.json` — 잠그기 **전** 상태를 담고 있다.
이 파일이 없으면 되돌릴 수 없다. 여러 번 돌려도 최초 상태를 잃지 않도록 병합한다.

⚠️ `directUseAllowed` 를 새로 잠그는 것은 원장님 확인이 필요한 조작이다(스키마 주석).
이 결함군에 대해서는 **2026-08-18 원장님이 「잠그고 → 회수」로 확정**했다.

---

## 6. 관련 문서

- `08-import-ledger.md` §1.5 — 그림 결손이 만든 「중복 88그룹」 착시. 중복 판정에 그림·정답을 같이 넣으라는 교훈
- `09-figure-engine-guide.md` §5 — 완료본 그림은 재작도가 아니라 **원본 오려오기**
- `10-handoff.md` §8.6 — 남은 그림 과제(표시 형태 D-07 미확정, 공통 지문 그림 이동 기능 필요)
- `12-discard-candidates.md` — 「그림 없음」 폐기 후보

---

## 5. 곁가지 결함 — **그림은 있는데 «어느 보기의 것인지» 모른다**

§0 의 유실과 **다른 결함**이다. 그림은 붙어 있다. 그런데 지면에
`① [그림] ② [그림]` 이라고 찍히고 그래프는 그 위에 따로 쌓여서,
**어느 그래프가 ①인지 지면에 없다.** 학생은 못 고른다.

조사·회수: [`tracks/reports/choice-figures.md`](./tracks/reports/choice-figures.md)

| 구분 | 건수 | 상태 |
|------|-----:|------|
| 대상 (출제 가능 풀) | **147** | 열쇠 다섯으로 셈 |
| └ **자동 회수** | **97** | 원본 지면 좌표에서 짝을 되찾음. **전량 육안 확인** |
| └ 사람확인 | 7 | **잠그지 않는다** — 사람이 보면 되는 것이지 못 쓰는 게 아니다 |
| └ 되찾지 못함 | **43** | 원장님 확정으로 **출제 제외** (아래) |

### 5.1 왜 이 원장에 같이 적나

**같은 컬럼(`directUseAllowed`)을 잠근다.** 두 잠금이 서로를 모르면
한쪽을 되돌릴 때 다른 쪽이 풀린다. 그래서

- 짝 제외 스크립트는 **그림 유실 원장에 이미 있는 행을 잠그지 않는다.**
- 되돌릴 때는 **지금 값이 자기가 쓴 값(false)일 때만** 되돌린다.

### 5.2 명령

```bash
# 상태 보기 (드라이런이 기본 — DB 를 안 쓴다)
npx tsx scripts/qa/apply-choice-figure-discard.ts

# 적용 / 되돌리기
ALLOW_UNIT_FIX=1 npx tsx scripts/qa/apply-choice-figure-discard.ts --apply
ALLOW_UNIT_FIX=1 npx tsx scripts/qa/apply-choice-figure-discard.ts --revert
```

원장: `scripts/qa/reports/choice-figure-discard-lock.json` (행마다 이전 값 + 사유 + 부류)

### 5.3 43건은 **되살릴 수 있다** — 부류별로 길이 다르다

| 부류 | 건수 | 되살릴 길 |
|------|-----:|------|
| 짝을 못 찾음 | 36 | 원본을 다시 구한다. **그중 19건은 HWP 변환본만 있으면 된다** |
| 문항 병합 | 6 | 재이관 (08 §5.1) |
| 머리말 오염 | 1 | 본문 손질 |

> ⚠️ **2026-08-18 기준 셋 다 드라이런까지만 했다.** 공유 DB 는 한 건도 안 바뀌었다.
> 원장님이 검수 시트를 보시고 승인하시면 적용한다.
